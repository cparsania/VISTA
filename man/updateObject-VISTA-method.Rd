% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/updateObject.R
\name{updateObject,VISTA-method}
\alias{updateObject,VISTA-method}
\title{Update a VISTA object to the current metadata schema}
\usage{
\S4method{updateObject}{VISTA}(object, ..., verbose = FALSE)
}
\arguments{
\item{object}{A \code{VISTA} object.}

\item{...}{Passed to other methods (unused).}

\item{verbose}{Logical; report what was migrated.}
}
\value{
A \code{VISTA} object stamped with the current schema version.
}
\description{
Brings a \code{VISTA} object created by an older version of the package up to the
current metadata layout. Missing metadata keys are back-filled with their
documented defaults, the schema tag is stamped to the running version, and
the migration is recorded in \code{metadata(x)$provenance$updates}.
}
\details{
Some content cannot be recovered by migration. In particular, objects built
before VISTA 1.2.0 did not retain raw counts, and normalized counts are not
invertible -- \code{\link[=counts]{counts()}} on such an object reports that a rebuild is
required rather than inventing values.

Objects carrying a \emph{newer} schema than the running package are left
untouched and reported by \code{\link[=validate_vista]{validate_vista()}} as an issue, because newer
metadata may carry semantics this version would misread.
}
\examples{
v <- example_vista()

# Simulate an object written by an older release.
S4Vectors::metadata(v)$vista_schema_version <- "0.9.0"
v2 <- BiocGenerics::updateObject(v)
S4Vectors::metadata(v2)$vista_schema_version

}
