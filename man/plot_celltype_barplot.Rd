% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconv.R
\name{plot_celltype_barplot}
\alias{plot_celltype_barplot}
\title{Plot cell-type composition as stacked bars}
\usage{
plot_celltype_barplot(
  x,
  group_column = NULL,
  samples = NULL,
  font_size = 12,
  cell_types = NULL,
  top_n = NULL,
  collapse_other = TRUE,
  normalize = c("sample", "none"),
  facet_by_group = TRUE
)
}
\arguments{
\item{x}{A VISTA object.}

\item{group_column}{Optional column in \code{sample_info(x)} used to facet/order samples.
If \code{NULL}, uses the active VISTA group column when available.}

\item{samples}{Optional character vector of sample names to include.}

\item{font_size}{Base font size.}

\item{cell_types}{Optional character vector of cell types to keep.}

\item{top_n}{Optional top-N cell types by mean score (ignored when \code{cell_types} is provided).}

\item{collapse_other}{Logical; collapse non-selected cell types into \code{"Other"}.}

\item{normalize}{One of \code{"sample"} (default; per-sample relative scores) or \code{"none"}.}

\item{facet_by_group}{Logical; facet by group when \code{group_column} is available.}
}
\value{
A ggplot object.
}
\description{
Plot cell-type composition as stacked bars
}
\examples{
mat <- matrix(rpois(20, lambda = 20), nrow = 5)
rownames(mat) <- paste0("gene", seq_len(5))
colnames(mat) <- paste0("sample", seq_len(4))
se <- SummarizedExperiment::SummarizedExperiment(
  assays = list(norm_counts = mat),
  colData = S4Vectors::DataFrame(
    cond = c("A", "A", "B", "B"),
    row.names = colnames(mat)
  ),
  rowData = S4Vectors::DataFrame(
    gene_id = rownames(mat),
    row.names = rownames(mat)
  )
)
v <- as_vista(se, group_column = "cond")
md <- S4Vectors::metadata(v)
md$cell_fractions <- data.frame(
  fibroblast = c(0.2, 0.3, 0.4, 0.5),
  epithelial = c(0.8, 0.7, 0.6, 0.5),
  row.names = colnames(mat)
)
S4Vectors::metadata(v) <- md
plot_celltype_barplot(v, group_column = "cond")
}
