% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_preparation.R
\name{match_vista_inputs}
\alias{match_vista_inputs}
\title{Match count and metadata inputs for VISTA}
\usage{
match_vista_inputs(
  counts,
  sample_info,
  column_geneid = NULL,
  sample_column = NULL,
  reorder = TRUE,
  drop_unmatched = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{counts}{Standardized counts from \code{read_vista_counts()}, or a compatible
count matrix/data frame.}

\item{sample_info}{Sample metadata from \code{read_vista_metadata()} or a data
frame coercible to that format.}

\item{column_geneid}{Optional gene identifier column for raw tabular counts.
Ignored when \code{counts} is the list output of \code{read_vista_counts()}.}

\item{sample_column}{Optional sample identifier column in \code{sample_info}.}

\item{reorder}{Logical; if \code{TRUE} (default), reorder \code{sample_info} to match
count columns.}

\item{drop_unmatched}{Logical; if \code{TRUE}, keep only the intersection of count
samples and metadata samples. Otherwise mismatches raise an error.}

\item{verbose}{Logical; print an informational alignment summary.}
}
\value{
A list with standardized \code{counts}, aligned \code{sample_info},
\code{column_geneid}, \code{sample_names}, \code{sample_name_map}, \code{row_data}, and a small \code{report}.
}
\description{
\code{match_vista_inputs()} aligns standardized counts and sample metadata so they
can be passed directly to \code{create_vista()}. It accepts the raw output from
\code{read_vista_counts()} or a count data frame/matrix plus sample metadata.
}
\examples{
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

cnt <- read_vista_counts(
  count_data[seq_len(25), ],
  format = "matrix",
  gene_id_column = "gene_id",
  verbose = FALSE
)
si <- read_vista_metadata(
  sample_metadata[sample_metadata$sample_names \%in\% cnt$sample_names, ],
  verbose = FALSE
)
matched <- match_vista_inputs(cnt, si, verbose = FALSE)

matched$column_geneid
identical(matched$sample_info$sample_names, colnames(matched$counts)[-1])
}
