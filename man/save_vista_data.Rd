% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exporting.R
\name{save_vista_data}
\alias{save_vista_data}
\title{Save VISTA tabular outputs to disk}
\usage{
save_vista_data(
  x,
  what = c("comparison", "comparisons", "norm_counts", "sample_info", "row_data",
    "deg_summary", "cutoffs"),
  file,
  sample_comparison = NULL,
  format = NULL,
  include_rownames = TRUE
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{what}{Character vector specifying which object(s) to export. Supported
values are \code{"comparison"}, \code{"comparisons"}, \code{"norm_counts"},
\code{"sample_info"}, \code{"row_data"}, \code{"deg_summary"}, and \code{"cutoffs"}.}

\item{file}{Output file path.}

\item{sample_comparison}{Optional comparison name used when \code{what} includes
\code{"comparison"}. Defaults to the first comparison in \code{comparisons(x)}.}

\item{format}{Output format. One of \code{"csv"}, \code{"tsv"}, \code{"rds"}, \code{"xlsx"}.
If \code{NULL}, inferred from \code{file} extension.}

\item{include_rownames}{Logical; include meaningful row identifiers (e.g.,
gene IDs or sample names) as explicit columns where applicable.}
}
\value{
Invisibly, the normalized output file path.
}
\description{
Exports selected data components from a \code{VISTA} object to CSV/TSV/RDS/XLSX.
}
\examples{
v <- example_vista()
save_vista_data(v, what = "comparison", file = tempfile(fileext = ".csv"), format = "csv")
}
