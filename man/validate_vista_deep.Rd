% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking.R
\name{validate_vista_deep}
\alias{validate_vista_deep}
\title{Deep validation of VISTA differential-expression fidelity}
\usage{
validate_vista_deep(
  counts,
  sample_info,
  column_geneid,
  group_column,
  group_numerator,
  group_denominator,
  methods = c("deseq2", "edger", "limma"),
  min_counts = 10,
  min_replicates = 1,
  log2fc_cutoff = 1,
  pval_cutoff = 0.05,
  p_value_type = "padj",
  covariates = NULL,
  design_formula = NULL,
  tolerance = 1e-08,
  return_plots = FALSE,
  error = TRUE
)
}
\arguments{
\item{counts}{Raw counts (matrix/data.frame) with a gene-id column and sample columns.}

\item{sample_info}{Data frame with sample metadata.}

\item{column_geneid}{Column name in \code{counts} that contains gene identifiers.}

\item{group_column}{Column in \code{sample_info} used to group samples.}

\item{group_numerator}{Character vector of numerator groups for pairwise comparisons.}

\item{group_denominator}{Character vector of denominator groups.}

\item{methods}{Character vector of backends to benchmark. Any subset of
\code{c("deseq2", "edger", "limma")}.}

\item{min_counts}{Minimum total counts per gene to retain.}

\item{min_replicates}{Minimum samples per group meeting filtering criteria.}

\item{log2fc_cutoff}{Absolute log2 fold-change threshold for DEG calling.}

\item{pval_cutoff}{P-value (or adjusted p-value) threshold.}

\item{p_value_type}{Either \code{"padj"} or \code{"pvalue"}.}

\item{covariates}{Optional character vector of additional sample_info columns.}

\item{design_formula}{Optional model formula (or formula string) including \code{group_column}.}

\item{tolerance}{Numeric tolerance used for floating-point comparisons.}

\item{return_plots}{Logical; if \code{TRUE}, return paired VISTA/reference plots
for MA, volcano, DEG count, and PCA views.}

\item{error}{Logical; if \code{TRUE}, abort when any discrepancy is detected.}
}
\value{
Invisibly returns the full benchmark report.
}
\description{
This validator combines \code{validate_vista()} with backend-to-backend numerical
equivalence checks against standalone DESeq2, edgeR, and limma runs.
}
\examples{
v <- example_vista()
si <- as.data.frame(sample_info(v))
data("count_data", package = "VISTA")
count_subset <- count_data[seq_len(500), c("gene_id", si$sample_names), drop = FALSE]

report <- validate_vista_deep(
  counts = count_subset,
  sample_info = si,
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control",
  methods = "limma",
  min_counts = 5,
  min_replicates = 1,
  error = FALSE
)

report$valid

\donttest{
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

target_groups <- c("control", "treatment1")
sample_subset <- sample_metadata[sample_metadata$cond_long \%in\% target_groups, ]
count_subset <- count_data[1:150, c("gene_id", sample_subset$sample_names)]

validate_vista_deep(
  counts = count_subset,
  sample_info = sample_subset,
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control",
  methods = c("deseq2", "edger"),
  min_counts = 5,
  min_replicates = 1
)
}
}
