% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_expression_scatter}
\alias{get_expression_scatter}
\title{Compare normalized expression between two samples or groups}
\usage{
get_expression_scatter(
  x,
  sample_x,
  sample_y,
  by = c("sample", "group"),
  group_column = NULL,
  genes = NULL,
  log_transform = TRUE,
  label_n = 20,
  label_size = 3,
  method = c("pearson", "spearman"),
  display_id = NULL
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{sample_x}{First sample or group to plot (character scalar).}

\item{sample_y}{Second sample or group to plot (character scalar).}

\item{by}{One of \code{"sample"} (use individual samples) or \code{"group"} (average
replicates within \code{group_column} before plotting). Default \code{"sample"}.}

\item{group_column}{Column in \code{sample_info} used when \code{by = "group"} (defaults
to stored grouping column).}

\item{genes}{Optional character vector of genes to include; defaults to all.}

\item{log_transform}{Logical; apply log2(x + 1) transform. Default \code{TRUE}.}

\item{label_n}{Integer; number of most divergent genes to label
(ranked by |x - y|). Set to 0 to disable labels.}

\item{label_size}{Numeric size for labeled genes.}

\item{method}{Correlation method for the subtitle; \code{"pearson"} (default) or
\code{"spearman"}.}

\item{display_id}{Optional column in \code{rowData(x)} to use for point labels
(fallback to gene_id/rownames when not available).}
}
\value{
A \code{ggplot2} object.
}
\description{
Plots gene-level expression for two selected samples or group means,
colours points by local density (viridis), labels the most divergent genes,
and reports Pearson/Spearman correlation.
}
\examples{
NULL
}
