% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_related.R
\name{get_expression_lollipop}
\alias{get_expression_lollipop}
\title{Plot mean expression per group as a lollipop chart}
\usage{
get_expression_lollipop(
  x,
  genes,
  sample_group = NULL,
  group_column = NULL,
  log_transform = TRUE,
  facet_scale = "free_y",
  point_size = 6,
  line_size = 1.2,
  label = TRUE,
  label_digits = 1,
  display_id = NULL,
  display_from = NULL,
  display_orgdb = NULL
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{genes}{Character vector (≤15 genes) to plot.}

\item{sample_group}{Optional character vector of groups (from \code{group_column}) to include.}

\item{group_column}{Optional column name in \code{sample_info} to use for grouping samples.}

\item{log_transform}{Logical; log2-transform expression before plotting.}

\item{facet_scale}{Scaling option passed to \code{facet_wrap()} when plotting multiple genes.}

\item{point_size}{Numeric size of the dots.}

\item{line_size}{Numeric size of the stems.}

\item{label}{Logical; draw numeric labels above the dots.}

\item{label_digits}{Integer; digits to show in labels when \code{label = TRUE}.}

\item{display_id}{Optional ID/column name to use for labels/facets. If supplied
and present in \code{rowData(x)}, those values are used; otherwise falls back to
ID mapping.}

\item{display_from}{Optional source ID type for mapping (used when \code{display_id}
is not found in \code{rowData}).}

\item{display_orgdb}{Optional \code{OrgDb} object used for ID mapping when
\code{display_id} is set but not found in \code{rowData}.}
}
\description{
Summarizes expression per group for a handful of genes using a stem-and-dot
(lollipop) plot. Values are averaged across replicates in each group; this
function does not show individual replicates. For per-sample display or
pairwise tests, use \code{get_expression_barplot()}.
}
