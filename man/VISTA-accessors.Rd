% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{VISTA-accessors}
\alias{VISTA-accessors}
\alias{comparisons}
\alias{deg_summary}
\alias{cutoffs}
\alias{norm_counts}
\alias{sample_info}
\alias{row_data}
\alias{group_colors}
\alias{group_palette}
\alias{comparisons,VISTA-method}
\alias{deg_summary,VISTA-method}
\alias{cutoffs,VISTA-method}
\alias{norm_counts,VISTA-method}
\alias{sample_info,VISTA-method}
\alias{row_data,VISTA-method}
\alias{group_colors,VISTA-method}
\alias{group_palette,VISTA-method}
\title{Accessor Methods for VISTA Object}
\usage{
\S4method{comparisons}{VISTA}(object, source = "active")

\S4method{deg_summary}{VISTA}(object, source = "active")

\S4method{cutoffs}{VISTA}(object)

\S4method{norm_counts}{VISTA}(object, summarise = FALSE)

\S4method{sample_info}{VISTA}(object)

\S4method{row_data}{VISTA}(object)

\S4method{group_colors}{VISTA}(object)

\S4method{group_palette}{VISTA}(object)
}
\arguments{
\item{object}{An object of class \code{VISTA}.}

\item{source}{Which DE result source to use for \code{comparisons()}/\code{deg_summary()}.
One of \code{"active"}, \code{"deseq2"}, \code{"edger"}, \code{"limma"}, or \code{"consensus"}.
\code{"active"} uses the currently selected source stored in metadata.}

\item{summarise}{Logical. If TRUE, returns mean-normalized counts grouped by the grouping column
stored in the \code{VISTA} object (e.g., condition or treatment). Default is FALSE.}
}
\value{
The content of the respective slot or processed data:
\describe{
\item{comparisons}{A named list of differential expression tables stored in \code{metadata(x)$de_results}.}
\item{deg_summary}{A named list of DEG summary tables stored in \code{metadata(x)$de_summary}.}
\item{cutoffs}{A list of analysis thresholds held in \code{metadata(x)$de_cutoffs} (empty list if absent).}
\item{norm_counts}{A matrix of normalized counts, optionally averaged by group.}
\item{sample_info}{A \code{DataFrame} of sample metadata.}
\item{row_data}{A \code{DataFrame} of gene-level annotation (e.g., baseMean, gene ID).}
\item{group_colors}{A named character vector of colours from \code{metadata(x)$group$colors}.}
\item{group_palette}{The qualitative palette name stored in \code{metadata(x)$group$palette}.}
}
}
\description{
These accessor functions expose the analysis components stored in a \code{VISTA} object.
Core expression matrices and annotations live in the underlying
\code{SummarizedExperiment}, while differential expression results, summaries, and
configuration details are kept inside \code{metadata(x)}.
}
\examples{
# Create example VISTA object
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

vista <- create_vista(
  counts = count_data[1:100, ],
  sample_info = sample_metadata[1:6, ],
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control"
)

# Access differential expression comparisons
comps <- comparisons(vista)
names(comps)
comparisons(vista, source = "active")

# View DEG summary statistics
deg_summary(vista)

# Get analysis cutoffs
cutoffs(vista)

# Access normalized counts
nc <- norm_counts(vista)
head(nc)

# Get group-summarized counts
nc_summary <- norm_counts(vista, summarise = TRUE)
head(nc_summary)

# Access sample metadata
sample_info(vista)

# Access gene-level annotations
head(row_data(vista))

# Get group colors
group_colors(vista)

# Get palette name
group_palette(vista)

# For method = "both", inspect method-specific outputs
\dontrun{
vista_both <- create_vista(
  counts = count_data,
  sample_info = sample_metadata,
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control",
  method = "both",
  result_source = "consensus"
)
comparisons(vista_both, source = "deseq2")
comparisons(vista_both, source = "edger")
vista_both <- set_de_source(vista_both, "edger")
}

}
\seealso{
\code{\link[=create_vista]{create_vista()}}, \code{\link[=as_vista]{as_vista()}}, \code{\link[=run_deseq_analysis]{run_deseq_analysis()}}
}
