% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_mds_plot}
\alias{get_mds_plot}
\title{Generate an MDS plot for samples in a VISTA object}
\usage{
get_mds_plot(
  x,
  sample_group = NULL,
  group_column = NULL,
  genes = NULL,
  top_n_genes = NULL,
  label_replicates = FALSE,
  label_size = 3,
  circle_size = 10,
  sample_colors = TRUE,
  shape_by = NULL,
  shape_values = NULL
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{sample_group}{Optional character vector of groups to include (based on the column specified by \code{group_column}).}

\item{group_column}{Optional column name in \code{sample_info} to use for grouping/filtering.}

\item{genes}{Optional character vector of gene identifiers to restrict the matrix.}

\item{top_n_genes}{Optional integer selecting the top variable genes to include.}

\item{label_replicates}{Logical; draw sample labels when \code{TRUE}.}

\item{label_size}{Numeric size of replicate labels when \code{label_replicates = TRUE}.}

\item{circle_size}{Numeric size for points.}

\item{sample_colors}{Logical; apply stored group colors when \code{TRUE}.}

\item{shape_by}{Optional column name in \code{sample_info} used to map point shape. When \code{NULL},
shapes are not mapped.}

\item{shape_values}{Optional vector of shapes passed to \code{scale_shape_manual()} when
\code{shape_by} is set. Use a named vector to map shapes to specific levels.}
}
\value{
An object returned by this function.
}
\description{
Runs classical multidimensional scaling on normalized counts, optionally
restricting to groups or genes.
}
\examples{
NULL
}
