% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_mds_plot}
\alias{get_mds_plot}
\title{Generate an MDS plot for samples in a VISTA object}
\usage{
get_mds_plot(
  x,
  sample_group = NULL,
  group_column = NULL,
  genes = NULL,
  top_n_genes = NULL,
  label = FALSE,
  label_size = 3,
  point_size = 10,
  shape_by = NULL,
  shape_values = NULL,
  color_by = NULL,
  use_vista_colors = NULL,
  palette = NULL,
  colors = NULL,
  use_group_colors = TRUE
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{sample_group}{Optional character vector of groups to include (based on the column specified by \code{group_column}).}

\item{group_column}{Optional column name in \code{sample_info} to use for grouping/filtering.}

\item{genes}{Optional character vector of gene identifiers to restrict the matrix.}

\item{top_n_genes}{Optional integer selecting the top variable genes to include.}

\item{label}{Logical; draw sample labels when \code{TRUE}.}

\item{label_size}{Numeric size of sample labels when \code{label = TRUE}.}

\item{point_size}{Numeric size for points.}

\item{shape_by}{Optional column name in \code{sample_info} used to map point shape. When \code{NULL},
shapes are not mapped.}

\item{shape_values}{Optional vector of shapes passed to \code{scale_shape_manual()} when
\code{shape_by} is set. Use a named vector to map shapes to specific levels.}

\item{color_by}{Optional column name in \code{sample_info} used for point colour.
Defaults to the active grouping column.}

\item{use_vista_colors}{Deprecated alias for \code{use_group_colors}. When
supplied, it overrides \code{use_group_colors}.}

\item{palette}{Optional qualitative palette name used when generating colours
for non-group metadata levels.}

\item{colors}{Optional named character vector of manual colours overriding
both \code{palette} and stored VISTA colours.}

\item{use_group_colors}{Logical; when \code{TRUE}, prefer the stored VISTA group
colours when colouring by the grouping column.}
}
\value{
An object returned by this function.
}
\description{
Runs classical multidimensional scaling on normalized counts, optionally
restricting to groups or genes.
}
\examples{
v <- example_vista()
p <- get_mds_plot(v)
print(p)
}
