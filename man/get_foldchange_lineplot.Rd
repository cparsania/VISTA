% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_foldchange_lineplot}
\alias{get_foldchange_lineplot}
\title{Fold-change line plot across comparisons}
\usage{
get_foldchange_lineplot(
  x,
  sample_comparisons,
  genes = NULL,
  km = NULL,
  facet_by = c("none", "cluster"),
  alpha = 0.5,
  show_summary = TRUE,
  summary_color = NULL,
  summary_linewidth = 1,
  summary_fun = c("median", "mean")
)
}
\arguments{
\item{x}{A \code{VISTA} object containing differential expression results.}

\item{sample_comparisons}{Character vector of comparison names to include.}

\item{genes}{Optional character vector of gene identifiers to plot. Defaults to all genes.}

\item{km}{Optional integer specifying the number of k-means clusters to compute; \code{NULL} disables clustering.}

\item{facet_by}{Faceting mode: \code{"none"} (default) or \code{"cluster"} when
k-means clustering is requested.}

\item{alpha}{Numeric alpha applied to individual gene lines.}

\item{show_summary}{Logical; overlay a summary line per cluster when \code{TRUE}.}

\item{summary_color}{Color used for the summary line. When \code{NULL}, uses
the first comparison color (if stored) for consistency across plots.}

\item{summary_linewidth}{Numeric line width for the summary line.}

\item{summary_fun}{Character string selecting \code{"median"} or \code{"mean"} for the summary statistic.}
}
\value{
An object returned by this function.
}
\description{
Plots log2 fold-change trajectories for selected genes across multiple comparisons,
optionally clustering genes.
}
\examples{
v <- example_vista()
comp <- names(comparisons(v))[1]
genes <- head(as.character(comparisons(v)[[comp]]$gene_id), 5)
p <- get_foldchange_lineplot(v, sample_comparison = comp, genes = genes)
print(p)
}
