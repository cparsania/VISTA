% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R,
%   R/functional_analysis.R
\name{get_go_enrichment}
\alias{get_go_enrichment}
\title{Run GO enrichment directly from a VISTA comparison}
\usage{
get_go_enrichment(
  x,
  sample_comparison,
  regulation = c("Up", "Down", "Both", "All"),
  ont = c("BP", "MF", "CC"),
  from_type = "SYMBOL",
  orgdb = NULL,
  species = "Mus musculus",
  background = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{VISTA} object with DE results.}

\item{sample_comparison}{Comparison name to use.}

\item{regulation}{One of \code{"Up"}, \code{"Down"}, \code{"Both"}, or \code{"All"}; selects genes.}

\item{ont}{GO ontology: \code{"BP"}, \code{"MF"}, or \code{"CC"}.}

\item{from_type}{Identifier type in the DE tables (default \code{"SYMBOL"}).}

\item{orgdb}{OrgDb object; defaults to mouse/human based on \code{species}.}

\item{species}{Species name to infer default OrgDb.}

\item{background}{Optional background gene set; default uses all features.}

\item{...}{Passed to \code{clusterProfiler::enrichGO()}.}
}
\value{
A list with \code{enrich} containing an \code{enrichResult}.
}
\description{
Run GO enrichment directly from a VISTA comparison
}
\examples{
\dontrun{
v <- example_vista()
comp <- names(comparisons(v))[1]
if (requireNamespace('org.Mm.eg.db', quietly = TRUE)) {
  out <- try(get_go_enrichment(v, sample_comparison = comp, ont = 'BP', from_type = 'ENSEMBL',
                               orgdb = org.Mm.eg.db::org.Mm.eg.db), silent = TRUE)
  if (!inherits(out, 'try-error')) out
}
}
}
