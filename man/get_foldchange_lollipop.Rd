% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_foldchange_lollipop}
\alias{get_foldchange_lollipop}
\title{Fold-change plotting helpers (overview)}
\usage{
get_foldchange_lollipop(
  x,
  sample_comparison,
  genes = NULL,
  sort_by = c("input", "log2fc", "abs_log2fc"),
  palette = NULL,
  point_size = 6,
  line_size = 1.2,
  label = TRUE,
  label_digits = 2,
  display_id = NULL,
  dodge_width = 0.5,
  facet_by = c("auto", "gene", "comparison", "none")
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{sample_comparison}{Character vector of length 1 or 2 naming the
comparison(s) to plot (must exist in \code{metadata(x)$de_results}).}

\item{genes}{Optional character vector of gene IDs to include. When \code{NULL},
all genes in the specified comparison(s) are shown.}

\item{sort_by}{How to sort genes on the y-axis: \code{"input"} (use supplied
order), \code{"log2fc"} (descending log2FC; for two comparisons uses the first
comparison), or \code{"abs_log2fc"} (descending max abs log2FC across
comparisons).}

\item{palette}{For a single comparison, named vector of colors for \code{"pos"},
\code{"neg"}, and \code{"zero"} sign classes (set to \code{NULL} to disable color by
sign). For two comparisons, a named or unnamed vector of colors with one
entry per comparison (defaults to a qualitative palette).}

\item{point_size}{Numeric size of dots.}

\item{line_size}{Numeric size of stems (linewidth).}

\item{label}{Logical; draw numeric labels next to the dots.}

\item{label_digits}{Integer; digits to show in labels when \code{label = TRUE}.}

\item{display_id}{Optional column in \code{rowData(x)} to use for gene labels.
Input gene matching still uses \code{gene_id}.}

\item{dodge_width}{Horizontal separation between comparisons when plotting
two comparisons on the same axis.}

\item{facet_by}{Faceting mode: \code{"auto"} (default), \code{"gene"},
\code{"comparison"}, or \code{"none"}.}
}
\value{
An object returned by this function.

A \code{ggplot2} object.
}
\description{
One-stop doc for fold-change plots:
\itemize{
\item \code{get_foldchange_barplot()}: log2FC by comparison (bars).
\item \code{get_foldchange_boxplot()}: log2FC distributions per comparison (boxes).
\item \code{get_foldchange_lollipop()}: log2FC stems/dots; supports 1–2 comparisons.
\item \code{get_foldchange_lineplot()}: log2FC trajectories across comparisons (optional clustering).
}
}
\details{
Shared arguments: \code{x} (\code{VISTA} with DE results), \code{sample_comparisons/sample_comparison},
\code{genes}, \code{display_id} for label mapping, \code{sort_by} (where supported),
faceting controls (\code{facet_*}), and comparison colours pulled from
Plot log2 fold changes as a lollipop chart (one or two comparisons)

Extracts log2 fold changes from stored differential expression results and
plots them as stems and dots, with labels and a zero reference line. You can
optionally provide two comparisons; in that case both comparisons are drawn
side-by-side, coloured by comparison.
}
\examples{
v <- example_vista()
comp <- names(comparisons(v))[1]
genes <- head(as.character(comparisons(v)[[comp]]$gene_id), 10)
p <- get_foldchange_lollipop(v, sample_comparison = comp, genes = genes)
print(p)
vista <- example_vista()
comp_name <- names(comparisons(vista))[1]
genes <- rownames(vista)[seq_len(3)]
get_foldchange_lollipop(vista, sample_comparison = comp_name, genes = genes)
}
