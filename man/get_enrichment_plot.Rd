% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R,
%   R/functional_analysis.R
\name{get_enrichment_plot}
\alias{get_enrichment_plot}
\title{Plot enrichment results using -log10(FDR)}
\usage{
get_enrichment_plot(x, top_n = 10, title = NULL)
}
\arguments{
\item{x}{An object of class \code{enrichResult}, \code{gseaResult}, or \code{compareClusterResult}.}

\item{top_n}{Integer; number of top terms to plot (per cluster for compareCluster).}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot2} object.
}
\description{
Generates a dot plot of enrichment results for \code{enrichResult}, \code{gseaResult},
or \code{compareClusterResult} objects (including those returned by
\code{\link[=enrichMsigDB]{enrichMsigDB()}}). Points are sized by gene/set count and coloured by
-log10(FDR). For compareCluster results, the plot is faceted by cluster with
top terms selected per cluster.
}
\examples{
\dontrun{
v <- example_vista()
com <- names(comparisons(v))[1]
if (requireNamespace('msigdbr', quietly = TRUE)) {
  ms <- try(get_msigdb_enrichment(v, sample_comparison = com, regulation = 'Up', from_type = 'ENSEMBL'), silent = TRUE)
  if (!inherits(ms, 'try-error') && !is.null(ms$enrich)) print(get_enrichment_plot(ms$enrich))
}
}
}
