% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-internal.R
\name{.categorize_deg_results}
\alias{.categorize_deg_results}
\title{Categorize Differential Expression Results (DESeq2 or edgeR)}
\usage{
.categorize_deg_results(
  de_results,
  log2fc_cutoff = 1,
  pval_cutoff = 0.05,
  p_value_type = c("padj", "pvalue", "FDR", "PValue")
)
}
\arguments{
\item{de_results}{A differential expression results object. Can be a DESeqResults object,
an edgeR \code{topTags()} table, or a data frame/tibble containing fold changes and p-values.}

\item{log2fc_cutoff}{Numeric; minimum absolute log2 fold change to classify a DEG. Default is 1.}

\item{pval_cutoff}{Numeric; p-value or FDR threshold to define significance. Default is 0.05.}

\item{p_value_type}{String; which column to use for filtering significance. One of \code{"padj"}, \code{"pvalue"}, \code{"FDR"}, \code{"PValue"}. Default is \code{"padj"}.}
}
\value{
A tibble with gene IDs, fold changes, p-values, and a new \code{regulation} column,
indicating the category of each gene:
\itemize{
\item \code{"Up"}: Genes with log2 fold change >= \code{log2fc_cutoff} and p-value <= \code{pval_cutoff}.
\item \code{"Down"}: Genes with log2 fold change <= -\code{log2fc_cutoff} and p-value <= \code{pval_cutoff}.
\item \code{"Other"}: Genes not meeting the above criteria.
}
}
\description{
This function classifies genes as "Up", "Down", or "Other" based on fold-change and p-value thresholds.
It supports input from DESeq2 (\code{DESeqResults}), edgeR (e.g., \code{topTags()}), or a general tibble/data frame.
}
\keyword{internal}
