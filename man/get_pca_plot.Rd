% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_related.R
\name{get_pca_plot}
\alias{get_pca_plot}
\title{PCA plot}
\usage{
get_pca_plot(
  x,
  sample_group = NULL,
  group_column = NULL,
  genes = NULL,
  top_n_genes = NULL,
  label_replicates = FALSE,
  label_size = 3,
  circle_size = 10,
  sample_colors = TRUE,
  shape_by = NULL,
  shape_values = NULL,
  sample.seed = 123,
  show_clusters = FALSE
)
}
\arguments{
\item{x}{A \code{VISTA} object containing normalized counts.}

\item{sample_group}{Optional character vector of group labels (taken from the column specified by
\code{group_column}, defaulting to the stored grouping column) used to subset samples prior to PCA. Use
\code{NULL} to include all samples.}

\item{group_column}{Optional column name in \code{sample_info} to use for grouping. Defaults to
the stored grouping column.}

\item{genes}{Optional character vector of gene identifiers to restrict the PCA input matrix.
When \code{NULL}, all genes are used.}

\item{top_n_genes}{Optional integer selecting the top most variable genes to include. Ignored
when \code{genes} is supplied.}

\item{label_replicates}{Logical; if \code{TRUE}, sample names are drawn next to the points.}

\item{label_size}{Numeric size of replicate labels when \code{label_replicates = TRUE}.}

\item{circle_size}{Numeric size of the plotted points.}

\item{sample_colors}{Logical; if \code{TRUE}, apply the stored group colors to the points.}

\item{shape_by}{Optional column name in \code{sample_info} used to map point shape. When \code{NULL},
shapes are not mapped.}

\item{shape_values}{Optional vector of shapes passed to \code{scale_shape_manual()} when
\code{shape_by} is set. Use a named vector to map shapes to specific levels.}

\item{sample.seed}{Deprecated/unused; retained for backward compatibility.}

\item{show_clusters}{Logical; add normal ellipses per group when \code{TRUE}.}
}
\value{
A ggplot object showing the first two PCs.
}
\description{
Uses normalized counts to compute principal components and plot samples,
optionally restricting to selected groups or genes.
}
\examples{
# Create VISTA object
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

vista <- create_vista(
  counts = count_data[1:200, ],
  sample_info = sample_metadata[1:6, ],
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control"
)

# Basic PCA plot
get_pca_plot(vista)

# With sample labels
get_pca_plot(vista, label_replicates = TRUE)

# Using top variable genes
get_pca_plot(vista, top_n_genes = 100)

# With confidence ellipses
get_pca_plot(vista, show_clusters = TRUE)

}
