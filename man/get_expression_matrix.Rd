% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnaseq_related.R
\name{get_expression_matrix}
\alias{get_expression_matrix}
\title{Retrieve an expression matrix from a VISTA object}
\usage{
get_expression_matrix(
  x,
  assay_name = "norm_counts",
  genes = NULL,
  samples = NULL,
  group_column = NULL,
  summarise = FALSE,
  transform = c("none", "log2", "zscore")
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{assay_name}{Assay to extract (default: \code{"norm_counts"}).}

\item{genes}{Optional character vector of gene IDs to keep.}

\item{samples}{Optional character vector of sample IDs (or group labels when
\code{summarise = TRUE}) to keep.}

\item{group_column}{Optional column in \code{sample_info} used when summarising
replicates. Defaults to the stored \code{group_column}.}

\item{summarise}{Logical; if \code{TRUE}, averages replicates per \code{group_column}.}

\item{transform}{One of \code{"none"}, \code{"log2"}, or \code{"zscore"} applied after
subsetting and optional summarisation. Z-scores are computed within the
selected samples/columns.}
}
\value{
A numeric matrix with genes in rows and samples (or groups) in columns.
}
\description{
Returns the specified assay (default \code{"norm_counts"}) with optional gene/sample
subsetting, replicate summarization by group, and simple transformations.
}
