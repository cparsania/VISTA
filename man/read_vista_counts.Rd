% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_preparation.R
\name{read_vista_counts}
\alias{read_vista_counts}
\title{Read and standardize count inputs for VISTA}
\usage{
read_vista_counts(
  x,
  format = c("auto", "matrix", "featurecounts", "star", "htseq", "tximport", "rsem"),
  gene_id_column = NULL,
  sample_columns = NULL,
  sample_names = NULL,
  annotation_columns = NULL,
  count_column = NULL,
  tx2gene = NULL,
  counts_from = c("counts", "abundance", "length"),
  drop_technical = TRUE,
  remove_special_rows = TRUE,
  make_unique_ids = FALSE,
  repair_sample_names = c("auto", "none"),
  return_type = c("list", "data.frame", "matrix"),
  verbose = TRUE
)
}
\arguments{
\item{x}{Count input. Supported values depend on \code{format} and include a
matrix, data frame, single file path, vector of file paths, or a
tximport-like list with \code{counts}, \code{abundance}, and/or \code{length}.}

\item{format}{Input format. One of \code{"auto"}, \code{"matrix"},
\code{"featurecounts"}, \code{"star"}, \code{"htseq"}, \code{"tximport"}, or \code{"rsem"}.}

\item{gene_id_column}{Optional gene identifier column in tabular inputs.
When omitted, VISTA uses common names such as \code{gene_id}/\code{Geneid}, or falls
back to rownames for matrices/data frames with unique rownames.}

\item{sample_columns}{Optional character vector of sample count columns to
retain from tabular inputs.}

\item{sample_names}{Optional sample names to use when \code{x} is a vector of
per-sample files.}

\item{annotation_columns}{Optional feature annotation columns to retain in the
returned \code{row_data}.}

\item{count_column}{Optional count column selector for formats that expose
multiple count choices. For STAR, use one of \code{"unstranded"},
\code{"stranded_first"}, or \code{"stranded_second"}. For RSEM, this defaults to
\code{"expected_count"}.}

\item{tx2gene}{Optional two-column mapping used to summarize transcript-level
tximport-like inputs to genes. The first column should contain transcript
IDs and the second column gene IDs.}

\item{counts_from}{Which matrix to extract from a tximport-like input:
\code{"counts"}, \code{"abundance"}, or \code{"length"}.}

\item{drop_technical}{Logical; when \code{TRUE}, drop known technical summary rows
from STAR/HTSeq inputs.}

\item{remove_special_rows}{Logical; alias for \code{drop_technical}, retained for
clarity in file-based imports.}

\item{make_unique_ids}{Logical; if \code{TRUE}, duplicate gene IDs are repaired
with \code{\link[=make.unique]{make.unique()}}. Otherwise duplicated gene IDs raise an error.}

\item{repair_sample_names}{Strategy for repairing sample column names.
\code{"auto"} (default) strips common file-path and alignment/count suffixes
when the repaired names are unique, while \code{"none"} leaves sample columns
unchanged. In automatic mode VISTA currently:
\itemize{
\item strips directory paths to the basename
\item uses the parent directory for generic quantification files such as
\code{quant.sf} or \code{abundance.tsv}
\item removes common RNA-seq output suffixes such as
\code{Aligned.sortedByCoord.out.bam}, \code{ReadsPerGene.out.tab},
\code{.genes.results}, \code{.isoforms.results}, \code{.bam}, and \code{.fastq.gz}
\item removes common lane/read suffixes such as \verb{_S1_L001_R1_001},
\verb{_L001_R2_001}, \verb{_R1}, and \verb{_R2}
}
Repaired names are only applied when they remain non-empty and unique.
Otherwise VISTA keeps the original count column names and records the
unchanged mapping in \code{sample_name_map}.}

\item{return_type}{Return \code{"list"} (default), standardized \code{"data.frame"},
or numeric \code{"matrix"}.}

\item{verbose}{Logical; print an informational import summary.}
}
\value{
If \code{return_type = "list"}, a list with:
\describe{
\item{counts}{A standardized count table with \code{gene_id} plus sample columns.}
\item{row_data}{Feature metadata aligned to the count table.}
\item{column_geneid}{Always \code{"gene_id"} for the standardized output.}
\item{sample_names}{Sample columns in the standardized count table.}
\item{sample_name_map}{A two-column mapping of original and repaired sample names.}
\item{input_format}{Resolved import format.}
\item{report}{Basic import summary.}
}

If \code{return_type = "data.frame"}, returns the standardized count table. If
\code{return_type = "matrix"}, returns a numeric matrix with gene IDs as rownames.
}
\description{
\code{read_vista_counts()} helps standardize common RNA-seq count inputs into a
count table that can be passed directly to \code{create_vista()}. It supports
plain matrices/data frames, featureCounts outputs, STAR gene counts,
HTSeq-count outputs, tximport-like lists, and RSEM gene result files.
}
\details{
Internally, VISTA uses a format-specific importer for each supported input
type, then normalizes the result into a common structure with:
\itemize{
\item a count table with a \code{gene_id} column plus sample columns
\item optional feature metadata in \code{row_data}
\item sample names inferred from columns or file names
\item an auditable \code{sample_name_map} showing original and repaired names
}
}
\examples{
data("count_data", package = "VISTA")

cnt <- read_vista_counts(
  count_data[seq_len(25), ],
  format = "matrix",
  gene_id_column = "gene_id"
)

head(cnt$counts[, seq_len(4)])
cnt$sample_names
}
