% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_foldchange_boxplot}
\alias{get_foldchange_boxplot}
\title{Plot fold-change distributions across comparisons}
\usage{
get_foldchange_boxplot(
  x,
  genes = NULL,
  sample_comparisons = NULL,
  display_id = NULL,
  display_from = NULL,
  display_orgdb = NULL,
  facet_scales = "free_x",
  facet_nrow = NULL,
  facet_ncol = NULL,
  facet_by = c("auto", "comparison", "none"),
  p.label = "p.signif",
  stats_group = FALSE,
  stats_method = "t.test"
)
}
\arguments{
\item{x}{A \code{VISTA} object containing differential expression results.}

\item{genes}{Optional character vector of gene IDs to include.}

\item{sample_comparisons}{Optional character vector of comparison names to plot.}

\item{display_id}{Optional ID/column name used to interpret \code{genes} and,
when possible, map fold-change gene identifiers to display-friendly labels.}

\item{display_from}{Optional source ID type for mapping when \code{display_id} is
not present in \code{rowData(x)}.}

\item{display_orgdb}{Optional \code{OrgDb} object used for identifier mapping when
\code{display_id} is not present in \code{rowData(x)}.}

\item{facet_scales}{Facet scales argument passed to \code{facet_wrap()} when
\code{facet_by != "none"} (default \code{"free_x"}).}

\item{facet_nrow, facet_ncol}{Optional layout passed to \code{facet_wrap()} when faceting.}

\item{facet_by}{Faceting mode: \code{"auto"} (default), \code{"comparison"}, or \code{"none"}.}

\item{p.label}{Label type passed to \code{ggpubr::stat_compare_means()}.}

\item{stats_group}{Logical; add pairwise statistical tests when \code{TRUE}.}

\item{stats_method}{Statistical method passed to \code{ggpubr::stat_compare_means()}.}
}
\value{
An object returned by this function.
}
\description{
Builds boxplots of log2 fold changes for selected genes and comparisons,
optionally adding statistics.
}
\examples{
v <- example_vista()
comp <- names(comparisons(v))[1]
genes <- head(as.character(comparisons(v)[[comp]]$gene_id), 10)
p <- get_foldchange_boxplot(v, sample_comparison = comp, genes = genes)
print(p)
}
