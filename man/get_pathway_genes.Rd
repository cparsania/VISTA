% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functional_analysis.R
\name{get_pathway_genes}
\alias{get_pathway_genes}
\title{Extract genes from enriched pathways}
\usage{
get_pathway_genes(
  x,
  pathways = NULL,
  top_n = 10,
  pathway_column = c("Description", "ID"),
  gene_column = c("auto", "geneID", "core_enrichment"),
  gene_sep = "/",
  return_type = c("long", "list", "vector")
)
}
\arguments{
\item{x}{An \code{enrichResult}/\code{gseaResult}, or a list containing element
\code{enrich} (e.g. output from \code{get_msigdb_enrichment()}).}

\item{pathways}{Optional character vector of pathway names to keep. Matches
against \code{pathway_column}.}

\item{top_n}{Number of top pathways to use when \code{pathways} is \code{NULL}.
Ranking uses \code{p.adjust} (then \code{pvalue}) when available. Default: \code{10}.}

\item{pathway_column}{Which enrichment column to match pathway names against:
\code{"Description"} (default) or \code{"ID"}.}

\item{gene_column}{Which column stores pathway members. \code{"auto"} (default)
uses \code{"geneID"} when present, otherwise \code{"core_enrichment"}.}

\item{gene_sep}{Delimiter used in pathway gene strings (default \code{"/"}).}

\item{return_type}{One of \code{"long"}, \code{"list"}, or \code{"vector"}.}
}
\value{
Depending on \code{return_type}:
\itemize{
\item \code{"long"}: data frame with \code{pathway_id}, \code{pathway}, and \code{gene}.
\item \code{"list"}: named list of character vectors (genes per pathway).
\item \code{"vector"}: unique character vector of genes.
}
}
\description{
Parses pathway-level gene members from an enrichment result and returns
either a long table, pathway-indexed list, or a unique vector of genes.
}
\examples{
if (requireNamespace("msigdbr", quietly = TRUE)) {
  vista <- example_vista()
  msig <- get_msigdb_enrichment(
    vista,
    sample_comparison = names(comparisons(vista))[1],
    regulation = "Both",
    msigdb_category = "H",
    from_type = "ENSEMBL"
  )
  pathway_tbl <- get_pathway_genes(msig, top_n = 5, return_type = "long")
  head(pathway_tbl)
}

\donttest{
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

vista <- create_vista(
  counts = count_data,
  sample_info = sample_metadata,
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control"
)

msig <- get_msigdb_enrichment(
  vista,
  sample_comparison = names(comparisons(vista))[1],
  regulation = "Up",
  species = "Homo sapiens",
  from_type = "ENSEMBL"
)

pathway_tbl <- get_pathway_genes(msig, top_n = 5, return_type = "long")
head(pathway_tbl)
}

}
