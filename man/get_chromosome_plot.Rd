% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_related.R
\name{get_chromosome_plot}
\alias{get_chromosome_plot}
\title{Plot gene positions along chromosomes using a TxDb}
\usage{
get_chromosome_plot(
  x,
  txdb,
  keytype = "GENEID",
  id_column = NULL,
  genes = NULL,
  value_column = NULL,
  comparison = NULL,
  group_value = NULL,
  label_top_n = 20,
  display_id = NULL,
  display_from = NULL,
  display_orgdb = NULL,
  line_length = 0.02,
  line_width = 0.6,
  filter_chrom = NULL,
  value_label = NULL,
  use_data_range = FALSE,
  force_fc_limits = FALSE,
  scale_mode = c("diverging", "sequential"),
  scale_low = NULL,
  scale_mid = NULL,
  scale_high = NULL,
  scale_midpoint = 0
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{txdb}{A TxDb object (e.g., from \pkg{GenomicFeatures}).}

\item{keytype}{Key type in the TxDb matching \code{id_column} (default \code{"GENEID"}).}

\item{id_column}{Optional column in \code{rowData(x)} used to match to TxDb keys.
When \code{NULL}, rownames(x) are used as keys.}

\item{genes}{Optional character vector of gene IDs to label (alternative to
\code{label_top_n}). When provided, all genes are plotted but only these
are labeled. Defaults to \code{NULL} (no explicit label set).}

\item{value_column}{Optional column in \code{rowData(x)} used for colouring.}

\item{comparison}{Optional comparison name; when supplied, uses \code{log2fc} from
\code{metadata(x)$de_results[[comparison]]} for colouring. If multiple
comparisons are provided, one panel per comparison is shown (log2FC
clipped to +/-2).}

\item{group_value}{Optional group label (from \code{group_column}); when supplied,
uses mean expression for that group for colouring (assay \code{norm_counts}).}

\item{label_top_n}{Integer; number of genes with largest |value| (or random if
no value) to label. Ignored when \code{genes} is provided. Set to 0 to disable labels.}

\item{display_id}{Optional column in \code{rowData(x)} to use for point labels
(fallback to gene_id/rownames).}

\item{display_from}{Optional source ID type for mapping when \code{display_id} is
not present in \code{rowData(x)}.}

\item{display_orgdb}{Optional \code{OrgDb} object used for identifier mapping when
\code{display_id} is not present in \code{rowData(x)}.}

\item{line_length}{Horizontal half-length (in megabases) of the tick used to
mark each gene position. Default \code{0.02}. Increase for longer ticks.}

\item{line_width}{Line width of the tick marks. Default \code{0.6}.}

\item{filter_chrom}{Optional character vector of chromosomes to keep (e.g.,
\code{c("chr1","chr2")}). When \code{NULL}, all chromosomes returned by the TxDb are shown.}

\item{value_label}{Optional legend title override for the colour scale.}

\item{scale_mode}{Colour scale mode: \code{"diverging"} (default) or
\code{"sequential"}.}

\item{scale_low}{Optional low-end colour override.}

\item{scale_mid}{Optional midpoint colour override (used with diverging scale).}

\item{scale_high}{Optional high-end colour override.}

\item{scale_midpoint}{Numeric midpoint passed to
\code{ggplot2::scale_color_gradient2()} when \code{scale_mode = "diverging"}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Retrieves gene coordinates on the fly from a user-supplied TxDb and plots
selected genes along chromosomes, optionally colouring by a numeric value and
labeling the most variable genes.
}
\details{
The \code{genes} argument is only used as an explicit label set (all genes
are still plotted). Values in \code{genes} must match either the rownames of
\code{x} or the values in \code{id_column} when that is supplied. For example,
if \code{id_column = "ENTREZID"}, then \code{genes} should contain Entrez IDs
to be labeled.
When multiple comparisons are supplied, \code{value_column} and \code{group_value} are
ignored and the plot is facetted by comparison with a fixed +/-2 log2FC scale.
}
\keyword{internal}
