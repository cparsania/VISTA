% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_deg_alluvial}
\alias{get_deg_alluvial}
\title{Plot alluvial diagram showing gene regulation transitions across comparisons}
\usage{
get_deg_alluvial(x, sample_comparisons, show_other = FALSE)
}
\arguments{
\item{x}{A VISTA object (DE results in \code{comparisons(x)}).}

\item{sample_comparisons}{Character vector of comparison names to include (>= 2).}

\item{show_other}{Logical; include "Other" genes. Default FALSE.}
}
\value{
A ggplot object.
}
\description{
Plot alluvial diagram showing gene regulation transitions across comparisons
}
\examples{
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")
si <- as.data.frame(sample_metadata[1:8, ], stringsAsFactors = FALSE)
trt_idx <- which(si$cond_long == "treatment1")
si$cond_long[trt_idx] <- rep(c("treatment1", "treatment2"), length.out = length(trt_idx))
si$groups <- si$cond_long
v <- create_vista(
  counts = count_data[1:120, c("gene_id", si$sample_names)],
  sample_info = si,
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = c("treatment1", "treatment2"),
  group_denominator = c("control", "control"),
  min_counts = 5,
  min_replicates = 1
)
if (requireNamespace('ggalluvial', quietly = TRUE)) {
  p <- get_deg_alluvial(v, sample_comparisons = names(comparisons(v)))
  print(p)
}
}
