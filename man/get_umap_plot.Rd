% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_related.R
\name{get_umap_plot}
\alias{get_umap_plot}
\title{Generate a UMAP plot for samples in a VISTA object}
\usage{
get_umap_plot(
  x,
  sample_group = NULL,
  group_column = NULL,
  color_by = NULL,
  genes = NULL,
  top_n = NULL,
  label = FALSE,
  label_size = 3,
  point_size = 10,
  shape_by = NULL,
  shape_values = NULL,
  n_neighbors = 15,
  min_dist = 0.1,
  metric = "euclidean",
  seed = 123,
  use_vista_colors = NULL,
  palette = NULL,
  colors = NULL,
  use_group_colors = TRUE,
  top_n_genes = NULL,
  max_genes = 20
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{sample_group}{Optional character vector of groups to include (based on
\code{group_column}).}

\item{group_column}{Optional column name in \code{sample_info} used for
filtering/grouping. Defaults to the stored grouping column.}

\item{color_by}{Optional column name in \code{sample_info} used for point color.
Defaults to \code{group_column}.}

\item{genes}{Optional character vector of gene identifiers to restrict the matrix.}

\item{top_n}{Optional integer selecting top variable genes to include.}

\item{label}{Logical; draw sample labels when \code{TRUE}.}

\item{label_size}{Numeric label size when \code{label = TRUE}.}

\item{point_size}{Numeric point size.}

\item{shape_by}{Optional column name in \code{sample_info} used to map point shape.}

\item{shape_values}{Optional vector passed to \code{scale_shape_manual()} when
\code{shape_by} is set.}

\item{n_neighbors}{UMAP \code{n_neighbors} parameter.}

\item{min_dist}{UMAP \code{min_dist} parameter.}

\item{metric}{UMAP distance metric.}

\item{seed}{Integer random seed passed to UMAP.}

\item{use_vista_colors}{Deprecated alias for \code{use_group_colors}. When
supplied, it overrides \code{use_group_colors}.}

\item{palette}{Optional qualitative palette name used when generating colours
for non-group metadata levels.}

\item{colors}{Optional named character vector of manual colours overriding
both \code{palette} and stored VISTA colours.}

\item{use_group_colors}{Logical; when \code{TRUE}, prefer the stored VISTA group
colours when colouring by the grouping column.}

\item{top_n_genes}{Deprecated; use \code{top_n}.}

\item{max_genes}{Maximum number of genes accepted in \code{genes} (default 20).}
}
\value{
A ggplot object with UMAP1/UMAP2 coordinates.
}
\description{
Runs UMAP on normalized counts, optionally restricting to selected groups or
genes. UMAP is intended for exploratory sample-level structure.
}
\examples{
if (requireNamespace("uwot", quietly = TRUE)) {
  vista <- example_vista()
  get_umap_plot(vista, top_n_genes = 50)
}
}
