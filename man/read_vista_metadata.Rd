% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_preparation.R
\name{read_vista_metadata}
\alias{read_vista_metadata}
\title{Read and standardize sample metadata for VISTA}
\usage{
read_vista_metadata(
  x,
  sample_column = NULL,
  required_columns = NULL,
  drop_empty = TRUE,
  standardize_names = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{Sample metadata as a data frame or file path.}

\item{sample_column}{Optional column to use as \code{sample_names}. If omitted,
VISTA uses an existing \code{sample_names} column, non-default rownames, or
common aliases such as \code{sample}, \code{sample_id}, or \code{Run}.}

\item{required_columns}{Optional character vector of columns that must be
present after import.}

\item{drop_empty}{Logical; if \code{TRUE}, remove columns that are entirely \code{NA}
or empty strings.}

\item{standardize_names}{Logical; if \code{TRUE}, coerce the final \code{sample_names}
column to character and set rownames to match it.}

\item{verbose}{Logical; print an informational import summary.}
}
\value{
A data frame suitable for use as \code{sample_info} in \code{create_vista()}.
}
\description{
\code{read_vista_metadata()} standardizes a sample sheet for use as
\code{sample_info} in \code{create_vista()}. It infers or creates the required
\code{sample_names} column using the same conventions VISTA already accepts in the
constructor.
}
\examples{
data("sample_metadata", package = "VISTA")

si <- read_vista_metadata(sample_metadata[seq_len(6), ])
head(si$sample_names)
}
