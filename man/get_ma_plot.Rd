% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_ma_plot}
\alias{get_ma_plot}
\title{Generate MA plot from a VISTA object}
\usage{
get_ma_plot(
  x,
  sample_comparison,
  point_size = 1.2,
  alpha = 0.6,
  colors = c(Up = "#a40000", Down = "#16317d", Other = "gray70"),
  label_n = 0,
  label_size = 3,
  repair_genes = FALSE,
  display_id = NULL,
  display_from = NULL,
  display_orgdb = NULL
)
}
\arguments{
\item{x}{A \linkS4class{VISTA} object.}

\item{sample_comparison}{Character scalar naming the comparison to plot. Must match
one of \code{names(comparisons(x))}.}

\item{point_size}{Numeric point size. Default: 1.2.}

\item{alpha}{Numeric transparency (0-1). Default: 0.6.}

\item{colors}{Named character vector of colors for \code{"Up"},
\code{"Down"}, and \code{"Other"} genes.}

\item{label_n}{Integer number of genes to label. Default: 0.}

\item{label_size}{Text size for labels. Default: 3.}

\item{repair_genes}{Logical; if \code{TRUE}, attempt to shorten gene identifiers to
symbols by stripping prefixes. Default: \code{FALSE}.}

\item{display_id}{Optional ID/column name to use for labels. If supplied and
present in \code{rowData(x)}, those values are used; otherwise falls back to ID
mapping.}

\item{display_from}{Optional source ID type for mapping (used when
\code{display_id} is not found in \code{rowData}).}

\item{display_orgdb}{Optional \code{OrgDb} used for ID mapping when \code{display_id}
is set but not found in \code{rowData}.}
}
\value{
A \code{\link[ggplot2]{ggplot}} MA plot.
}
\description{
Create an MA plot (log2 fold change vs mean expression) for a selected
comparison contained in a VISTA object. Genes are coloured by their
regulation class and the top results can be optionally labeled with gene IDs.
}
\examples{
v <- example_vista()
p <- get_ma_plot(v, sample_comparison = names(comparisons(v))[1])
print(p)
}
