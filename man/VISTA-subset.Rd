% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset-methods.R
\name{VISTA-subset}
\alias{VISTA-subset}
\alias{[,VISTA,ANY,ANY-method}
\alias{[,VISTA,ANY,ANY,ANY-method}
\title{Subset a VISTA object}
\usage{
\S4method{[}{VISTA,ANY,ANY,ANY}(x, i, j, ..., drop = FALSE)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{i, j}{Row (gene) and column (sample) subscripts.}

\item{...}{Passed to the \code{SummarizedExperiment} method.}

\item{drop}{Ignored, as for \code{SummarizedExperiment}.}
}
\value{
A \code{VISTA} object.
}
\description{
Subsets the underlying \code{SummarizedExperiment} and keeps the analysis metadata
consistent with the result.
}
\details{
Row (gene) subsetting reindexes every stored differential-expression table --
the active \code{de_results}/\code{de_summary} and every entry of
\code{de_results_by_method}/\code{de_summary_by_method} -- to the retained genes, in the
new row order. DEG summaries are recounted from the retained rows.

Column (sample) subsetting leaves the DE tables untouched, because they are
analysis \emph{results} rather than per-sample data; recomputing them would
require re-running the model. Group and comparison colour maps are pruned to
the groups that still have samples, and a warning names any comparison whose
groups are no longer represented, since plots of that comparison no longer
correspond to the samples in the object.

Each subset appends an entry to \code{metadata(x)$provenance$subset_history}.
}
\examples{
v <- example_vista()

# DE tables follow the genes.
v10 <- v[seq_len(10), ]
nrow(comparisons(v10)[[1]])

# Sample subsetting prunes the group colour map.
v_ctrl <- v[, sample_info(v)$cond_long == "control"]
group_colors(v_ctrl)

}
