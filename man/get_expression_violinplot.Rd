% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_expression_violinplot}
\alias{get_expression_violinplot}
\title{Violin plot of expression values}
\usage{
get_expression_violinplot(
  x,
  genes = NULL,
  sample_group = NULL,
  group_column = NULL,
  log_transform = TRUE,
  display_id = NULL,
  display_from = NULL,
  display_orgdb = NULL,
  facet_scales = "free_y",
  stats_group = FALSE,
  p.label = "p.signif",
  comparisons = NULL,
  pool_genes = FALSE,
  by = "group",
  facet_by = c("auto", "gene", "none"),
  fill_by = NULL,
  sample_order = c("input", "group", "expression"),
  value_transform = NULL,
  summarise = FALSE
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{genes}{Optional character vector of gene IDs to include; defaults to
all genes selected by the plotting mode.}

\item{sample_group}{Optional subset of groups (values of \code{group_column}) to keep.}

\item{group_column}{Grouping column in \code{sample_info}; defaults to the stored grouping.}

\item{log_transform}{Logical; log2-transform expression values before plotting.}

\item{display_id}{Optional ID/column name to use for labels/facets. If
supplied and present in \code{rowData(x)}, those values are used.}

\item{display_from}{Optional source ID type for mapping (reserved for
compatibility with \code{\link[=get_expression_boxplot]{get_expression_boxplot()}}).}

\item{display_orgdb}{Optional \code{OrgDb} object used for ID mapping when
\code{display_id} is set but not found in \code{rowData} (reserved for compatibility
with \code{\link[=get_expression_boxplot]{get_expression_boxplot()}}).}

\item{facet_scales}{Scaling option passed to \code{facet_wrap()}.}

\item{stats_group}{Logical; add statistical comparisons between groups when \code{TRUE}.}

\item{p.label}{Label format for \code{ggpubr::stat_compare_means()}.}

\item{comparisons}{Optional list of specific group comparisons for \code{stat_compare_means()}.}

\item{pool_genes}{Logical; pool all selected genes into one violin per group.}

\item{by}{Plot unit. Violin plots currently support only \code{"group"}.}

\item{facet_by}{Faceting mode. Uses the same argument pattern as
\code{\link[=get_expression_boxplot]{get_expression_boxplot()}}, but \code{pool_genes = TRUE} falls back to \code{"none"}
because pooled violins already aggregate across genes.}

\item{fill_by}{Fill strategy. Uses the same values as
\code{\link[=get_expression_boxplot]{get_expression_boxplot()}}.}

\item{sample_order}{Ordering for sample-level display before values are
grouped into violins.}

\item{value_transform}{Deprecated compatibility alias. \code{"log2"} maps to
\code{log_transform = TRUE}, \code{"none"} maps to \code{FALSE}, and \code{"zscore"} applies a
per-gene z-score transform.}

\item{summarise}{Logical retained for compatibility. Violin plots always use
replicate-level values, so \code{summarise = TRUE} is ignored with a warning.}
}
\value{
A \code{ggplot2} object.
}
\description{
Mirrors the main user-facing arguments of \code{\link[=get_expression_boxplot]{get_expression_boxplot()}} so the
two geoms can be swapped with minimal code changes. Violin plots currently
keep group-based semantics (\code{by = "group"}) because a violin requires
replicate-level distributions within groups rather than one value per sample.
}
\examples{
v <- example_vista()
genes <- head(rownames(v), 4)
p <- get_expression_violinplot(v, genes = genes)
print(p)
}
