% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_related.R
\name{get_expression_joyplot}
\alias{get_expression_joyplot}
\title{Plot expression distributions as ridgelines}
\usage{
get_expression_joyplot(
  x,
  genes = NULL,
  sample_group = NULL,
  group_column = NULL,
  log_transform = TRUE,
  alpha = 0.7,
  scale = 1.2,
  y_by = c("group", "sample"),
  color_by = c("group", "sample")
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{genes}{Optional character vector of genes to display. Defaults to all genes.}

\item{sample_group}{Optional character vector specifying which groups (as defined by \code{group_column}) to include.}

\item{group_column}{Optional column name in \code{sample_info} used as the grouping variable.}

\item{log_transform}{Logical; apply log2(x + 1) transform before plotting.}

\item{alpha}{Numeric transparency for fills.}

\item{scale}{Numeric scaling factor for ridges (passed to \code{geom_density_ridges()}).}

\item{y_by}{Either \code{"group"} (default) or \code{"sample"} to choose the y-axis
(ridge) grouping.}

\item{color_by}{Either \code{"group"} (default) or \code{"sample"} to choose fill colors.}
}
\value{
A \code{ggplot2} object.
}
\description{
Shows per-group (or per-sample) expression distributions pooled across the
selected genes using ridge (joy) plots. Genes are pooled; no faceting to keep
shapes comparable.
}
