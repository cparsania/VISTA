% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functional_analysis.R
\name{get_enrichment_chord}
\alias{get_enrichment_chord}
\title{Chord diagram of enrichment gene--pathway relationships}
\usage{
get_enrichment_chord(
  x,
  vista = NULL,
  sample_comparison = NULL,
  pathways = NULL,
  top_n = 8,
  pathway_column = c("Description", "ID"),
  gene_column = c("auto", "geneID", "core_enrichment"),
  gene_sep = "/",
  min_pathways = 1,
  max_genes = 50,
  gene_order_by = c("none", "foldchange", "abs_foldchange"),
  gene_id_column = NULL,
  display_id = NULL,
  color_by = c("foldchange", "regulation", "pathway"),
  up_color = "#D73027",
  down_color = "#1A9850",
  other_color = "grey70",
  pathway_colors = NULL,
  transparency = 0.4,
  gap_degree = 2,
  label_cex = 0.7,
  title = NULL
)
}
\arguments{
\item{x}{An \code{enrichResult}, \code{gseaResult}, or \code{compareClusterResult} from
clusterProfiler, or a list containing an \code{enrich} element (e.g. output of
\code{\link[=get_msigdb_enrichment]{get_msigdb_enrichment()}}).}

\item{vista}{Optional \code{VISTA} object.
Required when \code{color_by} is \code{"foldchange"} or \code{"regulation"}.}

\item{sample_comparison}{Character scalar naming the DE comparison in \code{vista}
to pull log2FC values from.
Required when \code{vista} is supplied.}

\item{pathways}{Optional character vector of pathway names to include.
Matches against \code{pathway_column}.}

\item{top_n}{Number of top pathways to display when \code{pathways} is \code{NULL}
(default \code{8}).}

\item{pathway_column}{Column in the enrichment result to match pathway names:
\code{"Description"} (default) or \code{"ID"}.}

\item{gene_column}{Column storing gene members: \code{"auto"} (default),
\code{"geneID"}, or \code{"core_enrichment"}.}

\item{gene_sep}{Delimiter separating genes in \code{gene_column} (default \code{"/"}).}

\item{min_pathways}{Minimum number of pathways a gene must appear in to be
shown. Set to \code{2} to display only hub genes shared across terms. Default
\code{1} (show all genes).}

\item{max_genes}{Maximum number of genes to display (default \code{50}). A safety
cap for readability.}

\item{gene_order_by}{Order of gene sectors in the chord plot:
\code{"none"} (default), \code{"foldchange"} (descending log2FC),
or \code{"abs_foldchange"} (descending absolute log2FC).
Fold-change based ordering requires \code{vista} + \code{sample_comparison}.}

\item{gene_id_column}{Column in \code{rowData(vista)} used to map enrichment
gene IDs to \code{vista} rownames (for FC lookup).}

\item{display_id}{Column in \code{rowData(vista)} providing display-friendly
gene names.}

\item{color_by}{How to colour chords: \code{"foldchange"} (continuous gradient),
\code{"regulation"} (Up / Down / Other), or \code{"pathway"} (source pathway).
Falls back to \code{"pathway"} when \code{vista} is \code{NULL}.}

\item{up_color}{Colour for up-regulated genes (default \code{"#D73027"}).}

\item{down_color}{Colour for down-regulated genes (default \code{"#1A9850"}).}

\item{other_color}{Colour for non-significant genes (default \code{"grey70"}).}

\item{pathway_colors}{Optional named vector of colours for pathway sectors.
When \code{NULL}, colours are generated from an HCL palette.}

\item{transparency}{Chord transparency, 0--1 (default \code{0.4}).}

\item{gap_degree}{Gap between sectors in degrees (default \code{2}).}

\item{label_cex}{Text size for sector labels (default \code{0.7}).}

\item{title}{Optional plot title.}
}
\value{
Invisibly returns a list with:
\describe{
\item{gene_data}{Tibble of genes with pathway membership and (optionally)
fold-change values.}
\item{hub_genes}{Character vector of genes appearing in two or more
pathways.}
}
The chord diagram is drawn as a side effect.
}
\description{
Draws a chord diagram linking genes to the enriched pathways they belong to.
Chords can be coloured by fold-change, regulation direction, or source
pathway.
}
\details{
The plot reveals \strong{hub genes} (those driving multiple enriched terms) and
pathway redundancy (terms sharing many genes).
This complements \code{\link[=get_enrichment_plot]{get_enrichment_plot()}} (which shows significance) and
\code{\link[=get_pathway_heatmap]{get_pathway_heatmap()}} (which shows expression patterns).
}
\examples{
v <- example_vista()
msig <- get_msigdb_enrichment(
  v,
  sample_comparison = names(comparisons(v))[1],
  regulation = "Both",
  msigdb_category = "H",
  from_type = "ENSEMBL"
)
get_enrichment_chord(msig, top_n = 5)

\donttest{
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

vista <- create_vista(
  counts = count_data[1:200, ],
  sample_info = sample_metadata[1:6, ],
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control"
)

msig <- get_msigdb_enrichment(
  vista,
  sample_comparison = names(comparisons(vista))[1],
  regulation = "Up", from_type = "ENSEMBL"
)

# Simple: pathway-coloured chords
get_enrichment_chord(msig)

# With fold-change colouring
get_enrichment_chord(
  msig, vista = vista,
  sample_comparison = names(comparisons(vista))[1],
  color_by = "foldchange"
)

# Hub genes only (shared across 2+ pathways)
pw_long <- get_pathway_genes(msig, return_type = "long")
if (any(table(pw_long$gene) >= 2)) {
  get_enrichment_chord(msig, min_pathways = 2)
}
}

}
