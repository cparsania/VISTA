% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconv.R
\name{get_celltype_group_dotplot}
\alias{get_celltype_group_dotplot}
\title{Plot group-level deconvolution scores as dot plot}
\usage{
get_celltype_group_dotplot(
  x,
  group_column = NULL,
  cell_types = NULL,
  top_n = 12,
  summary_fun = c("mean", "median"),
  errorbar = c("se", "sd", "none"),
  add_points = TRUE,
  point_size = 2.5,
  base_size = 12,
  error = NULL
)
}
\arguments{
\item{x}{A VISTA object.}

\item{group_column}{Column in \code{sample_info(x)} that defines groups.
If \code{NULL}, uses the active VISTA group column when available.}

\item{cell_types}{Optional character vector of cell types to include.}

\item{top_n}{Number of top cell types by mean score when \code{cell_types} is \code{NULL}.}

\item{summary_fun}{One of \code{"mean"} or \code{"median"} for group summary.}

\item{errorbar}{Error-bar type: \code{"se"}, \code{"sd"}, or \code{"none"}.}

\item{add_points}{Logical; overlay sample-level jittered points.}

\item{point_size}{Point size for summary points.}

\item{base_size}{Base font size.}

\item{error}{Deprecated; use \code{errorbar}.}
}
\value{
A ggplot object.
}
\description{
Plot group-level deconvolution scores as dot plot
}
\examples{
mat <- matrix(rpois(20, lambda = 20), nrow = 5)
rownames(mat) <- paste0("gene", seq_len(5))
colnames(mat) <- paste0("sample", seq_len(4))
se <- SummarizedExperiment::SummarizedExperiment(
  assays = list(norm_counts = mat),
  colData = S4Vectors::DataFrame(
    cond = c("A", "A", "B", "B"),
    row.names = colnames(mat)
  ),
  rowData = S4Vectors::DataFrame(
    gene_id = rownames(mat),
    row.names = rownames(mat)
  )
)
v <- as_vista(se, group_column = "cond")
md <- S4Vectors::metadata(v)
md$cell_fractions <- data.frame(
  fibroblast = c(0.2, 0.3, 0.4, 0.5),
  epithelial = c(0.8, 0.7, 0.6, 0.5),
  row.names = colnames(mat)
)
S4Vectors::metadata(v) <- md
get_celltype_group_dotplot(v, group_column = "cond")
}
