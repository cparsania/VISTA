% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_vista}
\alias{validate_vista}
\title{Validate a VISTA object}
\usage{
validate_vista(x, level = c("core", "full"), error = TRUE)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{level}{Validation depth. One of \code{"core"} or \code{"full"}.}

\item{error}{Logical; if \code{TRUE}, aborts on validation failures. If \code{FALSE},
returns the validation report and emits a warning when issues are found.}
}
\value{
Invisibly returns a list with fields \code{valid}, \code{level}, and \code{issues}.
}
\description{
Performs structural and metadata contract checks on a \code{VISTA} object.
Use \code{level = "core"} for invariants that all VISTA objects must satisfy,
or \code{level = "full"} to additionally validate method-specific metadata used
by advanced workflows.
}
\examples{
mat <- matrix(rnorm(60), nrow = 10)
rownames(mat) <- paste0("gene", seq_len(nrow(mat)))
colnames(mat) <- paste0("sample", seq_len(ncol(mat)))
se <- SummarizedExperiment::SummarizedExperiment(
  assays = list(norm_counts = mat),
  colData = S4Vectors::DataFrame(
    cond = rep(c("A", "B"), each = 3),
    row.names = colnames(mat)
  ),
  rowData = S4Vectors::DataFrame(
    gene_id = rownames(mat),
    row.names = rownames(mat)
  )
)
v <- as_vista(se, group_column = "cond")
validate_vista(v)
}
