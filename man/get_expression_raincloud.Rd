% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_expression_raincloud}
\alias{get_expression_raincloud}
\title{Raincloud plot of expression values}
\usage{
get_expression_raincloud(
  x,
  genes = NULL,
  sample_group = NULL,
  group_column = NULL,
  by = "group",
  value_transform = c("log2", "zscore", "none"),
  summarise = FALSE,
  facet_by = c("auto", "gene", "none"),
  fill_by = NULL,
  facet_nrow = NULL,
  facet_ncol = NULL,
  sample_order = c("input", "group", "expression"),
  rain_side = c("r", "l", "f", "f1x1", "f2x2"),
  id.long.var = NULL,
  alpha = 0.5,
  point_size = 1.5,
  p.label = "p.signif",
  stats_group = FALSE,
  stats_method = "t.test",
  label = FALSE,
  label_column = "gene",
  label_size = 3,
  label_max_overlaps = 50,
  display_id = NULL,
  display_from = NULL,
  display_orgdb = NULL
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{genes}{Optional character vector of gene IDs to include; defaults to
all genes selected by the plotting mode.}

\item{sample_group}{Optional subset of groups (values of \code{group_column}) to keep.}

\item{group_column}{Grouping column in \code{sample_info}; defaults to the stored grouping.}

\item{by}{Plot unit. Violin plots currently support only \code{"group"}.}

\item{value_transform}{Deprecated compatibility alias. \code{"log2"} maps to
\code{log_transform = TRUE}, \code{"none"} maps to \code{FALSE}, and \code{"zscore"} applies a
per-gene z-score transform.}

\item{summarise}{Logical; when \code{TRUE}, collapse replicates within each
group for each gene (one value per gene per group). This is useful for
pooled multi-gene raincloud plots where each dot represents one gene-level
summary in a group.}

\item{facet_by}{Faceting mode. Uses the same argument pattern as
\code{\link[=get_expression_boxplot]{get_expression_boxplot()}}, but \code{pool_genes = TRUE} falls back to \code{"none"}
because pooled violins already aggregate across genes.}

\item{fill_by}{Fill mapping. Uses the same values as
\code{\link[=get_expression_boxplot]{get_expression_boxplot()}}, including discrete sample metadata columns.}

\item{facet_nrow, facet_ncol}{Optional layout passed to \code{facet_wrap()} when faceting.}

\item{sample_order}{Ordering for sample-level display before values are
grouped into violins.}

\item{rain_side}{Side specification passed to \code{ggrain::geom_rain()}; one of
\code{"r"}, \code{"l"}, \code{"f"}, \code{"f1x1"}, or \code{"f2x2"}.}

\item{id.long.var}{Optional column name passed to \code{ggrain::geom_rain()} as
\code{id.long.var} to identify repeated measurements.}

\item{alpha}{Alpha for jittered points.}

\item{point_size}{Point size for jittered points.}

\item{p.label}{Label type passed to \code{ggpubr::stat_compare_means()}.}

\item{stats_group}{Logical; add pairwise statistical tests when \code{TRUE}.}

\item{stats_method}{Statistical method passed to \code{ggpubr::stat_compare_means()}.}

\item{label}{Logical; add text labels to points using \code{ggrepel}.}

\item{label_column}{Column name in the plotting data used for labels.
Defaults to \code{"gene"} for expression raincloud plots.}

\item{label_size}{Text size for point labels.}

\item{label_max_overlaps}{Maximum overlaps passed to \code{ggrepel::geom_text_repel()}.}

\item{display_id}{Optional ID/column name to use for labels. If supplied and
present in \code{rowData(x)}, those values are used; otherwise falls back to ID mapping.}

\item{display_from}{Optional source ID type for mapping (used when
\code{display_id} is not found in \code{rowData}).}

\item{display_orgdb}{Optional \code{OrgDb} object used for ID mapping when
\code{display_id} is set but not found in \code{rowData}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Uses \code{ggrain::geom_rain()} to combine a half-violin, boxplot, and jittered
points per sample/group to show distribution, summary, and individual values.
}
\details{
\code{id.long.var} controls which repeated unit is connected by lines in
\code{ggrain::geom_rain()}.

Recommended usage for expression raincloud plots:
\itemize{
\item \code{id.long.var = NULL} (default): best for clean distribution summaries.
\item \code{id.long.var = "gene"}: best when plotting a small number of genes and
showing gene-level trajectories across x levels.
\item \code{id.long.var = "<subject_id_column>"}: best for paired/repeated-measure
designs when a subject ID exists in \code{sample_info}.
\item \code{id.long.var = "sample"} or the grouping variable is usually less
informative and can over-connect points.
\item Point labels (\code{label = TRUE}) work best with \code{facet_by = "none"} or
a small number of genes.
}

For identifier display consistency with other VISTA plotting functions, set
\code{display_id} (for example, \code{"SYMBOL"}). When provided, \code{genes} can be given
in that ID space, and default point labels use the mapped display IDs.
}
\examples{
v <- example_vista()
genes <- head(rownames(v), 5)
p <- get_expression_raincloud(v, genes = genes, summarise = TRUE)
print(p)
}
