% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_related.R
\name{get_expression_barplot}
\alias{get_expression_barplot}
\title{Plot gene expression as barplots}
\usage{
get_expression_barplot(
  x,
  genes,
  sample_group = NULL,
  group_column = NULL,
  log_transform = TRUE,
  stats_group = FALSE,
  facet_scale = "free_y",
  facet_scales = facet_scale,
  facet_nrow = NULL,
  facet_ncol = NULL,
  p.label = "p.signif",
  comparisons = NULL,
  display_id = NULL,
  display_from = NULL,
  display_orgdb = NULL,
  by = c("group", "sample"),
  sample_order = c("input", "group", "expression"),
  fill_by = NULL,
  facet_by = c("auto", "gene", "none"),
  max_genes = 25
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{genes}{Character vector (<=25 genes) to plot.}

\item{sample_group}{Optional character vector of groups (from \code{group_column}) to include.}

\item{group_column}{Optional column name in \code{sample_info} to use for grouping samples.}

\item{log_transform}{Logical; log2-transform expression before plotting.}

\item{stats_group}{Logical; add statistical comparisons between groups when \code{TRUE}.}

\item{facet_scale}{Scaling option passed to \code{facet_wrap()} (deprecated; use \code{facet_scales}).}

\item{facet_scales}{Facet scales argument passed to \code{facet_wrap()} when faceting by gene.}

\item{facet_nrow, facet_ncol}{Optional layout passed to \code{facet_wrap()} when faceting.}

\item{p.label}{Label format for \code{ggpubr::stat_compare_means()}.}

\item{comparisons}{Optional list of specific group comparisons for \code{stat_compare_means()}.}

\item{display_id}{Optional ID/column name to use for labels/facets. If supplied
and present in \code{rowData(x)}, those values are used; otherwise falls back to
ID mapping.}

\item{display_from}{Optional source ID type for mapping (used when \code{display_id}
is not found in \code{rowData}).}

\item{display_orgdb}{Optional \code{OrgDb} object used for ID mapping when
\code{display_id} is set but not found in \code{rowData}.}

\item{by}{One of \code{"group"} (default; summarize replicates by group) or
\code{"sample"} (show one bar per sample).}

\item{sample_order}{Ordering used when \code{by = "sample"}:
\code{"input"} preserves the current sample order, \code{"group"} groups samples by
\code{group_column}, and \code{"expression"} ranks samples by mean expression across
the selected genes.}

\item{fill_by}{Fill mapping for \code{by = "sample"} barplots. Use \code{"group"}
(default) or any discrete column from the joined plotting data, such as a
sample metadata column or \code{"sample"}. Group-summary barplots
(\code{by = "group"}) only support group-based fill because each bar already
represents an aggregated group mean.}

\item{facet_by}{Faceting mode: \code{"auto"} (default; facet by gene when more
than one gene is requested), \code{"gene"}, or \code{"none"}. For multiple genes,
\code{"none"} falls back to \code{"gene"} to preserve readability.}

\item{max_genes}{Maximum number of genes accepted in one call (default 25).
Previously an undocumented hard cap.}
}
\value{
A \code{ggplot2} object.
}
\description{
Displays selected genes as grouped summary bars or individual sample-level
bars. By default, expression is summarized per group with mean ± SD bars.
With \code{by = "sample"}, each sample is drawn separately.
}
\examples{
# Create VISTA object
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

vista <- create_vista(
  counts = count_data[seq_len(200), ],
  sample_info = sample_metadata[seq_len(6), ],
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control"
)

# Plot expression for select genes
genes <- rownames(vista)[seq_len(3)]
get_expression_barplot(vista, genes = genes)

# With statistics
get_expression_barplot(vista, genes = genes, stats_group = TRUE)

# Without log transformation
get_expression_barplot(vista, genes = genes, log_transform = FALSE)

}
