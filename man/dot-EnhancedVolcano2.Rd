% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-internal.R
\name{.EnhancedVolcano2}
\alias{.EnhancedVolcano2}
\title{Enhanced volcano plot with smart column detection & coloring}
\usage{
.EnhancedVolcano2(
  toptable,
  lab,
  x = NULL,
  y = NULL,
  pCutoff = 1e-04,
  FCcutoff = 1.5,
  col_by_regul = TRUE,
  regulation_col = NULL,
  col_up = "#b2182b",
  col_down = "#2166ac",
  col_others = "#e0e0e0",
  return_keyvals = FALSE,
  ...
)
}
\arguments{
\item{toptable}{data.frame with DE results.}

\item{lab}{Character vector of labels (same length as nrow(toptable)).}

\item{x}{FC column name (auto-detected if NULL).}

\item{y}{P/P-adj column name (auto-detected if NULL).}

\item{pCutoff}{Numeric; P-value cutoff. Default 1e-4.}

\item{FCcutoff}{Numeric; symmetric scalar or c(down, up). Default 1.5.}

\item{col_by_regul}{Logical; color by regulation. Default TRUE.}

\item{regulation_col}{Optional column in \code{toptable} to use directly (expects levels Up/Down/Other).}

\item{col_up, }{col_down, col_others Colors.}

\item{return_keyvals}{Logical; invisibly return the computed \code{keyvals}. Default FALSE.}

\item{...}{Passed to EnhancedVolcano::EnhancedVolcano()}
}
\value{
ggplot object (and invisibly the \code{keyvals} if \code{return_keyvals=TRUE})
}
\description{
Internal helper that wraps EnhancedVolcano and colors points by regulation.
\itemize{
\item If \code{regulation_col} is present (e.g., "Up"/"Down"/"Other"), it is used.
\item Otherwise, regulation is derived from FC/P cutoffs.
}
}
\keyword{internal}
