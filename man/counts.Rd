% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts-methods.R
\name{counts}
\alias{counts}
\alias{counts,VISTA-method}
\title{Raw counts from a VISTA object}
\usage{
\S4method{counts}{VISTA}(object, ...)
}
\arguments{
\item{object}{A \code{VISTA} object.}

\item{...}{Ignored.}
}
\value{
A numeric matrix of raw counts with genes in rows and samples in
columns.
}
\description{
Returns the filtered raw count matrix stored alongside the normalized
counts. Objects built before VISTA 1.2.0, or with
\code{create_vista(keep_raw_counts = FALSE)}, do not carry one.
}
\details{
Normalized counts are not invertible, so \code{\link[BiocGenerics:updateObject]{BiocGenerics::updateObject()}}
cannot back-fill this assay for an older object; it has to be rebuilt with
\code{\link[=create_vista]{create_vista()}}.
}
\examples{
v <- example_vista()
counts(v)[seq_len(3), seq_len(3)]

}
