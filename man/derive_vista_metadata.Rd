% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_preparation.R
\name{derive_vista_metadata}
\alias{derive_vista_metadata}
\title{Derive starter sample metadata from count sample names}
\usage{
derive_vista_metadata(
  counts,
  column_geneid = NULL,
  sample_names = NULL,
  parser = c("auto", "split", "regex", "none"),
  split = "_",
  fields = NULL,
  pattern = NULL,
  sample_column = "sample_names",
  repair_sample_names = c("auto", "none"),
  return_type = c("data.frame", "template"),
  verbose = TRUE
)
}
\arguments{
\item{counts}{Count input accepted by \code{read_vista_counts()}, or the list
returned by \code{read_vista_counts()}.}

\item{column_geneid}{Optional gene identifier column for raw tabular count
inputs. Ignored when \code{counts} is the list output of \code{read_vista_counts()}.}

\item{sample_names}{Optional explicit sample names to derive metadata from.
When supplied, these override names extracted from \code{counts}.}

\item{parser}{Metadata parsing mode. \code{"auto"} tries a simple delimiter-based
split when sample names have a consistent structure. \code{"split"} uses
\code{split} explicitly. \code{"regex"} uses \code{pattern}. \code{"none"} returns only the
\code{sample_names} column.}

\item{split}{Delimiter used when \code{parser = "split"} or when \code{"auto"} chooses
split-based parsing.}

\item{fields}{Optional field names for parsed metadata columns. When omitted,
VISTA uses \code{part_1}, \code{part_2}, etc.}

\item{pattern}{Regular expression used when \code{parser = "regex"}. Capture
groups are mapped to \code{fields} in order.}

\item{sample_column}{Name of the sample identifier column in the returned
metadata. Default is \code{"sample_names"}.}

\item{repair_sample_names}{Strategy passed to \code{read_vista_counts()} when
sample names are taken from \code{counts}. One of \code{"auto"} or \code{"none"}.}

\item{return_type}{Return \code{"data.frame"} (default) or \code{"template"}. Both
return a data frame; \code{"template"} adds empty placeholder columns for
\code{group} and \code{batch}.}

\item{verbose}{Logical; print an informational derivation summary.}
}
\value{
A data frame containing \code{sample_names} plus any parsed metadata
columns.
}
\description{
\code{derive_vista_metadata()} creates a starter \code{sample_info} table from count
sample names. It is intended for projects where users have count columns but
do not yet have a separate metadata sheet. The derived table can be edited,
passed through \code{read_vista_metadata()}, and then aligned with
\code{match_vista_inputs()}.
}
\examples{
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

counts_in <- count_data[seq_len(8), c("gene_id", sample_metadata$sample_names[seq_len(6)]), drop = FALSE]
meta <- derive_vista_metadata(
  counts_in,
  column_geneid = "gene_id",
  parser = "regex",
  pattern = "SRR(\\\\d+)",
  fields = "run_id"
)
head(meta)
}
