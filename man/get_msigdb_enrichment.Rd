% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functional_analysis.R
\name{get_msigdb_enrichment}
\alias{get_msigdb_enrichment}
\title{Run MSigDB enrichment directly from a VISTA comparison}
\usage{
get_msigdb_enrichment(
  x,
  sample_comparison,
  regulation = c("Up", "Down", "Both", "All"),
  from_type = "SYMBOL",
  orgdb,
  msigdb_category = "H",
  msigdb_subcategory = NULL,
  species = "Mus musculus",
  background = NULL,
  col_genetype = "GENETYPE",
  feature_type = "protein-coding",
  ...
)
}
\arguments{
\item{x}{A \code{VISTA} object with DE results in \code{metadata(x)$de_results}.}

\item{sample_comparison}{Character scalar naming the comparison to use.}

\item{regulation}{One of \code{"Up"}, \code{"Down"}, \code{"Both"}, or \code{"All"}; selects which
genes to send to enrichment.}

\item{from_type}{Identifier type of the genes in the DE table (passed to
\code{\link[=enrichMsigDB]{enrichMsigDB()}}, default \code{"SYMBOL"}). Ensembl versions are stripped automatically.}

\item{orgdb}{An \code{OrgDb} object used for ID conversion (passed through). If omitted,
the default is chosen from \code{species} (mouse/human).}

\item{msigdb_category}{MSigDB category (e.g., \code{"H"}, \code{"C2"}, \code{"C5"}). Default \code{"H"}.}

\item{msigdb_subcategory}{Optional MSigDB sub-collection. Default \code{NULL}.}

\item{species}{Species name for MSigDB (default \code{"Mus musculus"}).}

\item{background}{Optional background gene set (passed to \code{\link[=enrichMsigDB]{enrichMsigDB()}}). Default \code{NULL}
uses all features in \code{x} (optionally filtered by \code{feature_type}).}

\item{col_genetype}{Column in \code{rowData(x)} used to filter background by gene type. Default \code{"GENETYPE"}.}

\item{feature_type}{Gene type to retain in the background when filtering. Default \code{"protein-coding"}.}

\item{...}{Additional arguments forwarded to \code{\link[=enrichMsigDB]{enrichMsigDB()}}.}
}
\value{
A list with \code{enrich} containing an \code{enrichResult}.
}
\description{
Convenience wrapper that pulls regulated genes from a stored differential
expression comparison in a \code{VISTA} object and runs \code{\link[=enrichMsigDB]{enrichMsigDB()}} on them.
}
\examples{
\dontrun{
# Create VISTA object
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

vista <- create_vista(
  counts = count_data,
  sample_info = sample_metadata,
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control"
)

# Run MSigDB enrichment on upregulated genes
msig_up <- get_msigdb_enrichment(
  vista,
  sample_comparison = "treatment1_VS_control",
  regulation = "Up",
  msigdb_category = "H",  # Hallmark gene sets
  from_type = "ENSEMBL"
)

# View results
head(msig_up$enrich)

# Visualize enrichment
get_enrichment_plot(msig_up$enrich)

# Enrichment for downregulated genes
msig_down <- get_msigdb_enrichment(
  vista,
  sample_comparison = "treatment1_VS_control",
  regulation = "Down",
  msigdb_category = "C2",  # Curated gene sets
  msigdb_subcategory = "CP:KEGG"  # KEGG pathways
)
}

}
