% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/VISTA-class.R
\name{as_vista}
\alias{as_vista}
\title{Coerce SummarizedExperiment to VISTA}
\usage{
as_vista(
  se,
  assay_name = "norm_counts",
  group_column,
  comparisons = list(),
  deg_summary = list(),
  cutoffs = list(),
  group_palette = "Dark 3",
  validate = TRUE
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} object.}

\item{assay_name}{Assay in \code{se} to use as \code{norm_counts} (default: \code{"norm_counts"}).}

\item{group_column}{Grouping column in \code{colData(se)}.}

\item{comparisons}{Optional named list of differential expression tables.}

\item{deg_summary}{Optional named list of DEG summary tables.}

\item{cutoffs}{Optional named list of threshold metadata.}

\item{group_palette}{Palette name for group colors.}

\item{validate}{Logical; run object validation before returning.}
}
\value{
A \code{VISTA} object.
}
\description{
Convert a pre-existing \code{SummarizedExperiment} into a valid
\code{VISTA} object.
}
\examples{
mat <- matrix(rnorm(60), nrow = 10)
rownames(mat) <- paste0("gene", seq_len(nrow(mat)))
colnames(mat) <- paste0("sample", seq_len(ncol(mat)))
se <- SummarizedExperiment::SummarizedExperiment(
  assays = list(norm_counts = mat),
  colData = S4Vectors::DataFrame(
    cond = rep(c("A", "B"), each = 3),
    row.names = colnames(mat)
  ),
  rowData = S4Vectors::DataFrame(
    gene_id = rownames(mat),
    row.names = rownames(mat)
  )
)
v <- as_vista(se, group_column = "cond")
v
}
