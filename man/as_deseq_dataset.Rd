% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts-methods.R
\name{as_deseq_dataset}
\alias{as_deseq_dataset}
\title{Convert a VISTA object to a DESeqDataSet}
\usage{
as_deseq_dataset(x, design = NULL)
}
\arguments{
\item{x}{A \code{VISTA} object carrying a raw \code{counts} assay.}

\item{design}{A model formula. Defaults to \verb{~ <group column>} using the
grouping column stored in the object.}
}
\value{
A \code{DESeqDataSet}.
}
\description{
Builds a \code{DESeq2::DESeqDataSet} from the raw counts retained in a \code{VISTA}
object, so an analysis can be continued or re-run with DESeq2 directly.
}
\details{
This is deliberately explicit rather than making
\code{DESeq2::DESeqDataSet(v, design)} work implicitly: the inherited
\code{SummarizedExperiment} method would pick up whichever assay comes first,
which is \code{norm_counts}, and fail because those values are not integers.

Counts are rounded to integers, as DESeq2 requires.
}
\examples{
v <- example_vista()
if (requireNamespace("DESeq2", quietly = TRUE)) {
  dds <- as_deseq_dataset(v)
  class(dds)
}
}
