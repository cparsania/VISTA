% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-internal.R
\name{set_rowdata}
\alias{set_rowdata}
\title{Set or append rowData annotations on a VISTA object}
\usage{
set_rowdata(
  x,
  annotations = NULL,
  orgdb = NULL,
  key_col = NULL,
  keytype = NULL,
  columns = c("SYMBOL", "GENENAME", "ENSEMBL", "ENTREZID", "TXCHROM", "TXSTART", "TXEND"),
  drop_version = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{annotations}{Optional data.frame/tibble/DataFrame with one row per gene
and a column containing the gene IDs to match against \code{rownames(x)}. If
omitted, annotations are pulled from \code{orgdb}.}

\item{orgdb}{Optional \code{OrgDb} object; when supplied (and \code{annotations} is
\code{NULL}), annotations are retrieved via \code{AnnotationDbi::select()}.}

\item{key_col}{Name of the column in \code{annotations} that holds the gene IDs.
If \code{NULL}, the function will try \code{gene_id}, \code{gene}, \code{ENSEMBL}, \code{SYMBOL}, or
use \code{rownames(annotations)}. Ignored when \code{annotations} is \code{NULL} and
\code{orgdb} is used.}

\item{keytype}{Key type for \code{orgdb} lookups (e.g., \code{"ENSEMBL"}, \code{"SYMBOL"}).
If \code{NULL}, inferred from \code{rownames(x)} (\code{ENSEMBL} if they start with "ENS",
otherwise \code{SYMBOL}).}

\item{columns}{Character vector of OrgDb columns to retrieve when using
\code{orgdb}. Default:
\code{c("SYMBOL","GENENAME","ENSEMBL","ENTREZID","TXCHROM","TXSTART","TXEND")}.
The \code{TXCHROM}/\code{TXSTART}/\code{TXEND} fields carry basic genomic coordinates when
available in the OrgDb.}

\item{drop_version}{Logical; if \code{TRUE}, strips Ensembl version suffixes (e.g.,
\code{.1}) from both the VISTA rownames and the key column/keys before matching.}

\item{overwrite}{Logical; if \code{TRUE}, replaces existing \code{rowData}. If \code{FALSE},
new columns are appended (overwriting by name when names collide).}
}
\value{
The updated \code{VISTA} object with \code{rowData} populated/appended.
}
\description{
Accepts a data.frame/tibble/DataFrame of gene-level annotations, aligns it to
the VISTA row order, and stores it in \code{rowData(x)}. Rows are matched by a
key column (default: tries \code{gene_id} or rownames); Ensembl version suffixes
can be stripped for matching.
}
\details{
OrgDb packages rarely include full genomic coordinates; the default
\code{TXCHROM/TXSTART/TXEND} columns may therefore be \code{NA} unless your OrgDb
provides them. For reliable coordinates, fetch them from an \code{EnsDb}/\code{TxDb}
(via \code{genes()} or \code{biomaRt}/AnnotationHub), build an annotation table keyed
on your gene IDs, and supply that via the \code{annotations} argument. When
fetching from an \code{OrgDb}, only columns available in that database will be
filled.
}
\examples{
vista <- example_vista()
custom_annot <- data.frame(
  gene_id = rownames(vista)[1:10],
  custom_info = paste0("Info_", seq_len(10))
)
vista2 <- set_rowdata(vista, annotations = custom_annot, key_col = "gene_id")
head(SummarizedExperiment::rowData(vista2)$custom_info)

\donttest{
# Load example VISTA object
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

vista <- create_vista(
  counts = count_data[1:100, ],
  sample_info = sample_metadata[1:6, ],
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control"
)

# Add annotations from OrgDb (human)
if (requireNamespace("org.Hs.eg.db", quietly = TRUE)) {
  vista <- set_rowdata(
    vista,
    orgdb = org.Hs.eg.db::org.Hs.eg.db,
    columns = c("SYMBOL", "GENENAME", "ENTREZID")
  )

  # View updated rowData
  head(SummarizedExperiment::rowData(vista))
}

# Or provide custom annotations
custom_annot <- data.frame(
  gene_id = rownames(vista)[1:10],
  custom_info = paste0("Info_", 1:10)
)
vista <- set_rowdata(vista, annotations = custom_annot, key_col = "gene_id")
}

}
