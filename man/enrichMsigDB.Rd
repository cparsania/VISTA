% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R,
%   R/functional_analysis.R
\name{enrichMsigDB}
\alias{enrichMsigDB}
\alias{enrichMsigDB,VISTA-method}
\title{Perform MSigDB over-representation analysis on a VISTA object}
\usage{
enrichMsigDB(
  x,
  gene_list,
  from_type = "SYMBOL",
  orgdb,
  msigdb_category = "H",
  msigdb_subcategory = NULL,
  species = "Mus musculus",
  background = NULL,
  col_genetype = "GENETYPE",
  feature_type = "protein-coding",
  ...
)
}
\arguments{
\item{x}{A VISTA object.}

\item{gene_list}{A non-empty character vector of gene identifiers.  This
argument is required and must not be \code{NULL}.}

\item{from_type}{Identifier type of the genes in \code{gene_list}. One of
\code{"SYMBOL"}, \code{"ENSEMBL"}, or \code{"ENTREZID"} (default \code{"SYMBOL"}). Ensembl IDs
have version suffixes stripped automatically.}

\item{orgdb}{An \code{OrgDb} object used for ID conversion. If omitted, a default
is chosen based on \code{species}: \code{org.Mm.eg.db} for mouse, \code{org.Hs.eg.db} for
human.}

\item{msigdb_category}{MSigDB category (e.g. \code{"H"}, \code{"C2"}, \code{"C5"}). Default \code{"H"}.}

\item{msigdb_subcategory}{Optional MSigDB sub-collection (e.g. \code{"BP"} for C5). Default \code{NULL}.}

\item{species}{Species name for MSigDB (default \code{"Mus musculus"}).}

\item{background}{Optional character vector of background gene IDs. If \code{NULL},
all features in the VISTA object (optionally filtered by \code{feature_type})
are used.}

\item{col_genetype}{Column name in \code{rowData(x)} used to filter the background
by gene type (default \code{"GENETYPE"}).}

\item{feature_type}{Gene type to retain in the background when \code{background}
is \code{NULL} (default \code{"protein-coding"}).}

\item{...}{Additional arguments to pass to \code{clusterProfiler::enricher()}.}
}
\value{
A list containing a single \code{enrichResult} object named \code{"enrich"}.
}
\description{
This function performs ORA on a set of genes from a VISTA object.  Gene
identifiers stored as “ENSG00000187634:SAMD11” will be split at the colon and
the appropriate part extracted based on \code{from_type}.  Genes are then
optionally converted via \code{orgdb} to match the identifier type required by
the MSigDB gene sets.
}
\examples{
NULL
}
