% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_expression_boxplot}
\alias{get_expression_boxplot}
\title{Plot gene expression distributions as boxplots}
\usage{
get_expression_boxplot(
  x,
  genes = NULL,
  sample_group = NULL,
  group_column = NULL,
  log_transform = TRUE,
  display_id = NULL,
  display_from = NULL,
  display_orgdb = NULL,
  facet_scales = "free_y",
  facet_nrow = NULL,
  facet_ncol = NULL,
  stats_group = FALSE,
  p.label = "p.signif",
  stat_comparisons = NULL,
  pool_genes = FALSE,
  by = "group",
  facet_by = "auto",
  fill_by = NULL,
  sample_order = c("input", "group", "expression"),
  comparisons = NULL
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{genes}{Optional character vector of genes to display (<=20). Defaults to all genes.}

\item{sample_group}{Optional character vector specifying which groups (as defined by \code{group_column}) to include.}

\item{group_column}{Optional column name in \code{sample_info} used as the grouping variable.}

\item{log_transform}{Logical; apply log2(x + 1) transform before plotting.}

\item{display_id}{Optional column in \code{rowData(x)} to use for gene labels (facets).}

\item{display_from}{Optional source ID type for mapping (used when \code{display_id}
is not found in \code{rowData}).}

\item{display_orgdb}{Optional \code{OrgDb} object used for ID mapping when
\code{display_id} is set but not found in \code{rowData}.}

\item{facet_scales}{Facet scales argument passed to \code{facet_wrap()} (default \code{"free_y"}).}

\item{facet_nrow, facet_ncol}{Optional layout passed to \code{facet_wrap()} when faceting.}

\item{stats_group}{Logical; add statistical comparisons between groups when
\code{TRUE}. Only supported when \code{by = "group"}.}

\item{p.label}{Label format for \code{ggpubr::stat_compare_means()}.}

\item{stat_comparisons}{Optional list of length-2 group pairs passed to
\code{ggpubr::stat_compare_means()} for significance brackets.}

\item{pool_genes}{Logical; when \code{TRUE}, pool selected genes into one
distribution per x-axis category (scenario 1).}

\item{by}{When \code{pool_genes = TRUE}, either \code{"group"} or \code{"sample"} (x-axis
and optional fill). When \code{pool_genes = FALSE}, either \code{"group"} or
\code{"gene"} (x-axis for per-gene distributions).}

\item{facet_by}{Faceting control: for pooled genes, \code{"group"} or \code{"none"}; for per-gene,
\code{"none"} (default) or \code{"gene"}. \code{"auto"} selects the most readable layout.}

\item{fill_by}{Fill mapping. Special values are \code{"group"}, \code{"gene"}, and
\code{"x"} (the plotted x-axis variable, when available). You may also supply a
discrete column from the joined plotting data, such as a sample metadata
column or \code{"sample"}.}

\item{sample_order}{Ordering used when sample names are shown on the x-axis:
\code{"input"}, \code{"group"}, or \code{"expression"}.}

\item{comparisons}{Deprecated; use \code{stat_comparisons}.}
}
\value{
A \code{ggplot2} object with one box per group (or per sample).
}
\description{
Displays per-sample or per-group distributions for selected genes using
normalized counts. The x-axis unit is controlled by \code{by}, and faceting is
controlled explicitly by \code{facet_by}.
}
\examples{
v <- example_vista()
genes <- head(rownames(v), 3)
p <- get_expression_boxplot(v, genes = genes)
print(p)
}
