% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_related.R
\name{get_expression_chromosome_plot}
\alias{get_expression_chromosome_plot}
\title{Chromosome plot for expression}
\usage{
get_expression_chromosome_plot(
  x,
  txdb,
  keytype = "GENEID",
  id_column = NULL,
  genes = NULL,
  value_column = NULL,
  value_from = c("rowdata", "assay"),
  value_assay = "norm_counts",
  facet_value_columns = FALSE,
  group_value = NULL,
  label_top_n = 20,
  display_id = NULL,
  line_length = 0.02,
  line_width = 0.6,
  filter_chrom = NULL,
  log_transform = TRUE,
  value_label = "log2(mean expr)"
)
}
\arguments{
\item{x}{A \code{VISTA} object.}

\item{txdb}{A TxDb object (e.g., from \pkg{GenomicFeatures}).}

\item{keytype}{Key type in the TxDb matching \code{id_column} (default \code{"GENEID"}).}

\item{id_column}{Optional column in \code{rowData(x)} used to match to TxDb keys.
When \code{NULL}, rownames(x) are used as keys.}

\item{genes}{Optional character vector of gene IDs to label (alternative to
\code{label_top_n}). When provided, all genes are plotted but only these
are labeled. Defaults to \code{NULL} (no explicit label set).}

\item{value_column}{Optional column in \code{rowData(x)} used for colouring.}

\item{value_from}{Source for \code{value_column} data: \code{"rowdata"} (default) or
\code{"assay"}. When \code{"assay"}, the selected assay column is copied into
\code{rowData} temporarily for colouring.}

\item{value_assay}{Assay name to pull values from when \code{value_from = "assay"}.
Default \code{"norm_counts"}.}

\item{facet_value_columns}{Ignored (kept for compatibility); multiple
\code{value_column}s are always arranged in a chromosome-by-column grid.}

\item{group_value}{Optional group label (from \code{group_column}); when supplied,
uses mean expression for that group for colouring (assay \code{norm_counts}).}

\item{label_top_n}{Integer; number of genes with largest |value| (or random if
no value) to label. Ignored when \code{genes} is provided. Set to 0 to disable labels.}

\item{display_id}{Optional column in \code{rowData(x)} to use for point labels
(fallback to gene_id/rownames).}

\item{line_length}{Horizontal half-length (in megabases) of the tick used to
mark each gene position. Default \code{0.02}. Increase for longer ticks.}

\item{line_width}{Line width of the tick marks. Default \code{0.6}.}

\item{filter_chrom}{Optional character vector of chromosomes to keep (e.g.,
\code{c("chr1","chr2")}). When \code{NULL}, all chromosomes returned by the TxDb are shown.}

\item{log_transform}{Logical; when \code{group_value} is used (and \code{value_column}
is \code{NULL}), apply log2(x + 1) before coloring.}

\item{value_label}{Optional legend title override for the colour scale.}
}
\value{
A \code{ggplot2} object or a list of \code{ggplot2} objects when multiple
\code{value_column}s are provided and \pkg{patchwork} is unavailable.
}
\description{
Convenience wrapper around \code{get_chromosome_plot()} for expression-based
colouring (optional group mean, rowData columns, or assay columns).
}
\details{
\itemize{
\item If multiple \code{value_column}s are supplied, one plot is produced per column
with its own colour legend titled by that column. Chromosomes are laid out
top-down in a single column: for each chromosome, plots for all
\code{value_column}s appear sequentially. Legends for each column are shown on
the first occurrence of that column. Requires \pkg{patchwork}; otherwise a
list of plots is returned.
\item Labels are kept consistent across \code{value_column}s: if \code{genes} is provided
it is used for all panels; otherwise the top \code{label_top_n} (by absolute
value in the first \code{value_column}) are used for all panels.
\item When \code{value_from = "assay"}, the specified assay column is copied into
\code{rowData} on the fly so it can be used for colouring.
\item When \code{group_value} is provided (and no \code{value_column}), colouring is based
on log2 mean expression for that group (assay \code{norm_counts}), using a
data-driven colour scale. Ignored when \code{value_column} is supplied.
}
}
