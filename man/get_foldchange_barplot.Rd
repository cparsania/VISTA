% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioccheck_roxygen_fixes.R, R/viz_related.R
\name{get_foldchange_barplot}
\alias{get_foldchange_barplot}
\title{Plot fold-change barplots across comparisons for selected genes}
\usage{
get_foldchange_barplot(
  x,
  genes,
  sample_comparisons = NULL,
  coord_flip = FALSE,
  display_id = NULL,
  sort_by = c("input", "log2fc", "abs_log2fc"),
  facet_scales = "free_y",
  facet_by = c("auto", "gene", "comparison", "none")
)
}
\arguments{
\item{x}{A \code{VISTA} object containing differential expression results.}

\item{genes}{Character vector of gene IDs to plot.}

\item{sample_comparisons}{Optional character vector of comparison names to include; defaults to all available.}

\item{coord_flip}{Logical; flip axes when \code{TRUE}.}

\item{display_id}{Optional column in \code{rowData(x)} to use for gene labels. Input
gene matching still uses \code{gene_id}.}

\item{sort_by}{How to order genes when faceting: \code{"input"} (use supplied order),
\code{"log2fc"} (descending log2FC of the first comparison), or \code{"abs_log2fc"}
(descending max absolute log2FC across comparisons).}

\item{facet_scales}{Facet scales argument passed to \code{facet_wrap()} when faceting
(default \code{"free_y"}).}

\item{facet_by}{Faceting mode: \code{"auto"} (default), \code{"gene"},
\code{"comparison"}, or \code{"none"}.}
}
\value{
An object returned by this function.

A \code{ggplot2} object.
}
\description{
Plots log2 fold changes for selected genes across one or more comparisons.
\code{facet_by} controls per-gene or per-comparison layout explicitly.
}
\examples{
v <- example_vista()
comp <- names(comparisons(v))[1]
genes <- head(as.character(comparisons(v)[[comp]]$gene_id), 10)
p <- get_foldchange_barplot(v, sample_comparison = comp, genes = genes)
print(p)
data("count_data", package = "VISTA")
data("sample_metadata", package = "VISTA")

vista <- create_vista(
  counts = count_data[1:200, ],
  sample_info = sample_metadata[1:6, ],
  column_geneid = "gene_id",
  group_column = "cond_long",
  group_numerator = "treatment1",
  group_denominator = "control"
)

genes <- rownames(vista)[1:3]
get_foldchange_barplot(vista, genes = genes)
get_foldchange_barplot(vista, genes = genes, facet_by = "gene")

}
